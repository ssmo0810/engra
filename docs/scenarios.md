# 특이 시나리오 정의

ENGRA가 감지해야 할 특이사항의 유형과 발생 조건을 정의합니다.
여기서 정의한 시나리오가 모의 데이터 생성기의 입력이 되고, 감지 성능(포함률) 측정의 기준이 됩니다.

- 담당: 임도영
- 목표: 24종 (알람 미발생 13종 + 알람 발생 7종 + **계획된 정상 운전 작업 3종** + **플랜트 정지 1종**)
- 주의: **실제 설비 태그명과 실제 운전 수치는 쓰지 않습니다.** 태그는 `AI-1201` 같은 가상 표기를 사용하고, 수치는 형태만 실제와 유사하게 잡습니다.

---

## 작성 방법

아래 표에 한 줄씩 채우면 됩니다. 모르는 칸은 비워두고 나중에 채워도 됩니다.

| 항목 | 설명 | 예시 |
| --- | --- | --- |
| 유형 | 어떤 종류의 이상인가 | 헌팅 / 드리프트 / 과압 / 과온 / 저온 / 임계 근접 / 연속 발생 |
| 대상 | 어떤 설비·계통에서 나타나는가 | 제품 순도, 압축기 토출온도, 밸브 개도 |
| 형태 | 값이 어떻게 움직이는가 | 30분간 진동 후 안정 / 2시간에 걸쳐 완만히 상승 |
| 크기 | 평소 대비 얼마나 벗어나는가 | 평소 변동폭의 4배 / 시간당 0.9℃ |
| 지속 | 얼마나 오래 가는가 | 29분 / 근무 내내 |
| 중요도 | 인수인계 관점에서 | 상 / 중 / 하 |
| 판단 근거 | 왜 그 중요도인가 | 알람에 안 걸리지만 재발 시 설비 손상 우려 |

`대상` 칸은 **가상 태그번호와 설명을 함께** 적습니다. 시나리오가 모의 데이터 생성기와 검출 알고리즘에 그대로 연결되도록 하기 위한 것입니다.
태그 정의는 [`tag_master.csv`](tag_master.csv) (계측 태그 53점), 배치는 [모의 DCS Overview 화면](asu_dcs_overview.html)에 있습니다.

---

## 시나리오 목록

| # | 유형 | 대상 | 형태 | 크기 | 지속 | 중요도 | 판단 근거 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 헌팅 | `AI-707` LP Column O2 순도 | **하향 편향** 진동 후 자연 안정 | 평소 변동폭의 6배, 하향 0.42%p (99.60 → 99.18) | 약 30분 | 상 | 알람 임계(L 99.0%)에는 미달하나 품질 직결 |
| 2 | 드리프트 | `TI-205` 압축기 토출온도 | 아주 완만한 상승, **근무 경계를 넘김** | 시간당 0.68℃ · 24시간 | 2근무 | 상 | 한 근무만 보면 알아채기 어려움. H(125℃) 도달 여부는 **끝나는 시각의 외기**에 달림 |
| 3 | 연속 발생 | `ZI-601` Cold Box 공급밸브 개도 | 단시간 변동 반복, **회차마다 진폭 증가** | 1회 ±6% → 2회 ±9% → 3회 ±13% | 근무 중 3회 | 중 | 반복 자체보다 **진폭이 커지는 추세**가 악화 신호 |
| 4 | 임계 근접 | `TI-401` Main HX 인입 Air 온도 | 상한 인근 체류 | 상한(20℃)의 95% | 40분 | 하 | 알람 없이 지나감 |
| 5 | 헌팅 | `FI-602` Cold Box 공급유량 | 급강하 후 10초 내 원복, 반복 | −5% (약 2,000 Nm3/h) | 10초 × 근무 후반 반복 | 중 | `PDI-702`·`TI-704`까지 동반 흔들림. 순간값이라 평균만 보면 놓침 |
| 6 | 파과 | `MI-302` 흡착탑 A 후단 수분 | 완만하다가 후반 급상승 | 0.8 → 3.4 ppm | 90분 | 상 | **H(3ppm) 초과로 알람 발생.** 차압 `PDI-303`도 동반 초과. 놓치면 콜드박스 결빙 |
| 7 | 고착 | `ZI-801` LOX 탱크 인입밸브 개도 | 지령이 바뀌어도 값이 고정 | 45.0%에서 미동 | 2시간 | 상 | 개도값 자체는 정상범위라 알람 없음. 레벨 추세로만 판별 |
| 8 | 서지 전조 | `PI-203` 압축기 토출압력 | 압력 상승과 유량 감소가 동시 | 압력 +3%, 유량 −6% | 25분 | 상 | 각각은 정상범위. 동시 발생이 서지 직전 패턴 |
| 9 | 저온 이탈 | `TI-403` Main HX Cold end 온도 | 완만한 상승 (냉각 부족) | 시간당 1.5℃ | 3시간 | 상 | 콜드박스 열밸런스 악화. 순도 저하로 이어짐 |
| 10 | 임계 근접 → 초과 | `LI-806` LIN 탱크 레벨 | 상한 인근 체류 후 후반에 돌파 | 87.3% → 89.0% (H 88%) | 2시간 | 중 | **H 초과로 알람 발생.** 출하 지연 시 벤트 손실로 직결 · ⚠ **전제 무너짐 — 아래 결함 절 참조** |
| 11 | 상관 붕괴 | `ZI-601` 개도 ↔ `FI-602` 유량 | 개도는 움직이는데 유량이 안 따라옴 | 상관 0.95 → 0.2 | 1시간 | 상 | **단일 태그로는 절대 잡히지 않음.** 밸브·유량계 고장 공통 신호 |
| 12 | 순간 이상값 | `MI-804` LOX 수분 분석값 | 말도 안 되는 값으로 순간 폭등 후 3초 내 원복 | 0.5 → 999 ppm (정상값의 약 2,000배) | 3초 × 근무 중 수 회 | 중 | 알람 on-delay보다 짧아 미발생. 분석기 셀·샘플링 라인 고장 전조 |
| 13 | 반복 헌팅 | `ZI-904` GN2 공급밸브 개도 | 공급압 제어 헌팅 | ±5% 진동 | 근무 중 5회 | 중 | 매회 짧아 알람 미달. 누적 벤트 손실 |
| 14 | 계단 변화 | `SI-507` 터빈 회전수 | 순간 강하 후 즉시 복귀 | −800 rpm | 20초 × 1회 | 상 | 지속시간이 짧아 알람 미달. 베어링 이상 전조 |
| 15 | 동반 이상 | `AI-707` LP 순도 → `AI-803` LOX 순도 | 두 태그가 시차를 두고 함께 저하 | 각 −0.3%p (3분 지연) | 2시간 | 상 | 연쇄가 확인되면 분석기 오작동이 아닌 실제 공정 이상 |
| 16 | 과온 | `TI-301` 흡착탑 A 내부온도 | 상승 후 고온 유지 | 32 → 55℃ (H 50℃) | 40분 | 상 | **알람 발생.** 재생 히터 제어 실패. 흡착제 열화로 이어짐 |
| 17 | 저압 | `PI-901` GN2 공급 압력 | 하강 후 저압 유지 | 4.85 → 4.35 bar (L 4.50) | 1시간 | 상 | **알람 발생.** 수요처 공급 차질 직결 |
| 18 | 고레벨 | `LI-701` HP Column 레벨 | 완만히 상승해 상한 돌파 | 25 → 44% (H 40%) | 90분 | 상 | **알람 발생.** 방치 시 캐리오버로 LP Column 순도 저하 |
| 19 | 차압 상승 | `PDI-103` 흡입 필터 차압 | 장시간 완만한 상승 | 0.012 → 0.033 bar (H 0.030) | 3시간 | 중 | **알람 발생.** 필터 막힘. 압축기 흡입 부하 증가 |
| 20 | 급증 | `JI-206` 압축기 소비 전력 | 급증 → 10분 유지 → **원복** | 3,240 → 3,670 kW (H 3,600) | 15분 | 중 | **알람 발생 후 해제.** 교대 시점엔 정상으로 돌아와 있어, 적지 않으면 다음 근무자가 모름 |
| **21** | **정상 작업 — 출하** | `LI-806` LIN 탱크 레벨 (`ZI-805` 개도) | LV-802 개도를 열어 2시간 출하 후 재충전 | 레벨 **27~40%p 하강** (최저 45.7%) · 개도 38.5 → 68.9% | 170분 | 중 | **이상이 아니라 계획된 작업.** 알람 미발생. 다음 근무자는 레벨이 왜 낮은지 알아야 함 |
| **22** | **정상 작업 — 출하** | `LI-802` LOX 탱크 레벨 (`ZI-801` 개도) | LV-801 개도를 열어 1.5시간 출하 후 재충전 | 레벨 **21~28%p 하강** (최저 30.0%) · 개도 45 → 70.5% | 150분 | 중 | 같은 구조. `L`(25%) 에 닿기 전 30% 에서 멈춤 |
| **23** | **정상 작업 — 계획 정지** | `JI-206` 압축기 전력 (44점 연쇄) | 압축기 감부하 3단 → 정지 1시간 → 재기동 2시간 | **44점 이탈 · 알람 39건** | 6시간 | 상 | **first-out = AIR COMPRESSOR K-201** (0초). 계획된 작업이므로 조치 불필요, 그러나 인계는 필수 |
| **24** | **플랜트 Trip** | `JI-206` 압축기 전력 상실 (44점 연쇄) | 30초 안에 압축기 붕괴 → 유닛별 2초~10분 연쇄 → 4시간 정지 → 재기동 | **44점 이탈 · 알람 39건 (HH/LL 포함)** | 6시간 | 상 | **first-out = AIR COMPRESSOR K-201** (2초). 원인 규명이 인계의 핵심 |

1~5번은 팀장님이 넣어주신 예시를 저희 태그 체계에 맞춰 태그번호를 붙인 것입니다.
6~15번은 ASU 공정에서 실제로 문제가 되는 유형으로 채웠습니다.
16~20번은 **한계값을 명확히 넘어 DCS 알람이 울리는 단순 유형**입니다 (Issue #1). 전파나 상관 구조 없이 단일 태그가 한계를 넘습니다. 모두 H는 넘되 HH는 넘지 않아, 설비 정지가 아니라 **근무 중 조치가 필요한 수준**으로 잡았습니다.

> **4번 참고** — 원래 예시는 "냉각수 입구온도"였는데 태그 마스터에 냉각수 계통이 없어 `TI-401`(Main HX 인입 Air 온도)로 대체했습니다. 냉각수 태그가 필요하면 태그 마스터에 추가하겠습니다.
>
> **1번 참고** — 진폭 6배로 최저값이 99.18%가 되면서, `AI-707`의 L 알람을 **99.2% → 99.0%** 로 조정했습니다(태그 마스터 반영 완료). 알람선이 정상범위 하한(99.0%)과 일치하게 되어, **정상범위를 벗어나는 순간 알람**이라는 기준으로 정리됩니다.
>
> **1번 하향 편향 (Issue #7)** — 처음에는 대칭 진동(±0.42%p)이었는데, 기저값 99.60%에서 위로 0.42%p를 더하면 **100.02%** 가 나왔습니다. 산소 순도가 100%를 넘을 수는 없습니다.
> 순도는 위로 갈 여유가 0.4%p뿐이므로 실제 헌팅도 **아래로만 크게 흔들립니다.** 그래서 기저값과 (기저값 − 0.42%p) 사이에서만 흔들리는 하향 편향 진동으로 바꿨습니다. 최저 99.18%는 그대로이고 상한은 절대 넘지 않습니다.
> 진동 함수 자체에도 **물리 여유 검사**를 넣어, 앞으로 어떤 태그에 진동을 걸어도 물리 한계를 넘지 않습니다.

---

## 정상 데이터 오탐 실측 (2026-08-29 · 기준선 6근무 · A안 수정 후 재측정)

**결함을 하나도 넣지 않은 데이터**에서 엔진이 무엇을 잡는지 쟀다. 잡히는 것은 정의상
**전부 오탐**이다. A안 수정 전후로 두 번 쟀다.

| | 수정 전 (드리프트 3단) | **수정 후 (2단)** |
| --- | --- | --- |
| 오탐 | 6건 / 6근무 = 근무당 1.00건 | **0건 / 6근무** |
| 그중 `LI-806` | 6건 (100%) — 근무 내내 676~694분 H 초과 | **0건** |
| 나머지 52개 태그 | 0건 | **0건** |
| 초안 항목 | 근무당 1개 | **0개** |
| 훑은 양 | 태그 53점 × 12시간 × 6근무 = **690만 점** | 같음 |

**정상 데이터 오탐은 0 건이다.** 엔진이 정상 구간에서 잘못 잡은 것이 하나도 없다.
수정 전의 1.00건/근무는 엔진이 아니라 **태그 마스터의 데이터 문제**였고, 아래
「해결된 결함」 절에서 고쳤다.

> 재측정 조건 — 깨끗한 기준선 6근무(`seed 20260814` · 08-14 06:00 ~ 08-17 06:00), 시나리오 0건,
> 알람 침범 0건. `ENGRA_LLM=off` 로 빈 저장소에 순서대로 적재하고 근무마다 `run`.

## 공식 대조 수치 (2026-08-29 · `data/` 2근무 · A안 수정 후)

```
근무                      이벤트    주입    탐지    오탐
2026-08-21-day           40    10    10     0
2026-08-21-night         39    12    12     2
------------------------------------------------------------------
주입 건 포함률   22/22  (100%)
시나리오 종 커버  20/20 종
시각 일치도 IoU  평균 0.71 · 중앙 0.79 · IoU≥0.5 17/22건   (검출 묶음 전체 0.48)
오탐             2건 / 이벤트 79건  (근무당 1.0건)
알람 선행 검출    7/11 건이 알람선 도달보다 먼저 잡힘  (최대 +706분)
```

### IoU 를 두 갈래로 나눠 낸다 (2026-08-29 결정)

```
시각 일치도 IoU  전체 22건 평균 0.71 · 중앙 0.79 · IoU≥0.5 17/22
  ├ 국소형 16건  평균 0.75 · 중앙 0.81 · IoU≥0.5 13/16   ← 시각까지 짚는다
  └ 추세형  6건  평균 0.61 · 중앙 0.71 · IoU≥0.5  4/6    ← 관찰 창을 적으므로 IoU 로 재는 지표가 아니다
```

**왜 나누는가.** 드리프트 검출은 창(60·120·240·720분)을 통째로 놓고 회귀선을 그어 기울기를
보는 방식이라, 결과 구간이 **그 창 전체**가 된다. 「12시간 내내 이상이었다」가 아니라
**「12시간 창으로 봤다」**는 뜻이다. 103분짜리 정답과 겹침을 재면 원리상 낮게 나온다 —
엔진이 시각을 못 짚은 것이 아니라 **자를 잘못 댄 것**이다.

실측 구간 길이가 그 근거다. **드리프트만 평균 247분·최대 720분**이고 나머지는 전부 최대 120분이라,
`tools/score.py` 는 **드리프트만 추세형**으로 분류한다.

| 종류 | 평균 구간 | 최대 | 분류 |
| --- | ---: | ---: | --- |
| 드리프트 | 247분 | **720분** | **추세형** |
| 고착 | 90분 | 120분 | 국소형 |
| 레벨시프트 | 50분 | 50분 | 국소형 |
| 임계근접 | 47분 | 82분 | 국소형 |
| 헌팅 | 37분 | 121분 | 국소형 |
| H 초과 | 26분 | 82분 | 국소형 |
| 이탈 | 1분 | 2분 | 국소형 |

> **전체 값을 반드시 함께 낸다.** 나누는 것은 감추려는 것이 아니라 서로 다른 것을 섞지
> 않으려는 것이다. 전체 0.71 을 빼고 0.75 만 쓰면 숫자를 고른 것이 된다.
>
> **나눈다고 숫자가 크게 좋아지지도 않는다** — 전체 0.71 → 국소형 0.75 로 0.04 오를 뿐이다.
> 그리고 IoU 가 낮은 4건 중 3건(`#5` 0.00 · `#14` 0.33 · `#1` 0.38)은 **국소형**이다.
> 「추세형이라 낮다」로 설명되는 것은 `#9`(0.14) 하나뿐이고, 그건 `#24` 외기 보정 결함으로
> 원인이 따로 규명돼 있다.

### 포함률과 IoU 를 함께 낸다

**포함률은 「태그를 맞혔는가」에 가까운 값이다.** 구간이 1초라도 겹치면 세기 때문에,
적대적 시험에서 **정답지를 통째로 60분 옮겨도 100% 가 나왔다.** 인계 목적으로는 그것으로
충분하지만 숫자를 쓸 때는 정의를 밝혀야 하므로 `tools/score.py` 가 **IoU 를 함께 낸다**.

- **최선 IoU** — 가장 잘 맞은 이벤트 하나 기준. 「시각을 짚은 이벤트가 있는가」
- **합집합 IoU** — 매칭 이벤트 전체 기준. 「검출 묶음이 군더더기 없는가」
- **시작 오차** — 최선 이벤트의 시작 시각이 정답보다 얼마나 이른가/늦은가 (분)

### IoU 가 드러낸 것 — `#9` 가 `LI-806` 과 같은 모양이다

| 시나리오 | IoU | 시작 오차 | 무슨 일인가 |
| --- | ---: | ---: | --- |
| `#9` Cold end 저온 이탈 | 0.14 | **−618분** | 근무 전체 드리프트 이벤트가 매칭됨 |
| `#5` 공급유량 헌팅 | 0.00 | +49분 | 2시간 주입인데 끝자락만 겹침 |
| `#8` 압축기 서지 전조 | 0.42 | −25분 | **25분 일찍 잡음 — 잘한 것** |
| `#1` 순도 헌팅 | 0.38 | +0분 | 시작은 정확, 구간 길이가 다름 |
| `#14` 터빈 회전수 순간 강하 | 0.33 | −0분 | 1분짜리라 IoU 가 원래 낮다 |

`#9` 는 **포함률로는 안 보이고 IoU 로만 보인다.** 파고들어 보니 `LI-806` 과는 원인이 달랐다 —
내 데이터 문제가 아니라 **엔진의 외기 보정 문제**다. 아래에 따로 적는다.

### `#9` 의 원인 — 후반 이상이 외기 계수를 오염시킨다 (`#24`)

같은 seed·같은 시각으로 **시나리오만 뺀** 야간 근무를 만들어 비교했다. 원본 차이는
주입분 **+2.60℃**(1.5℃/h × 1.73h) 뿐이다.

| | 원본 변화 | `adj` 변화 | 보정이 얹은 램프 | 12h 창 k | r |
| --- | ---: | ---: | ---: | ---: | ---: |
| 시나리오 0건 | −1.20℃ | +0.97℃ | **+2.17℃** | 0.83 | +0.28 |
| 시나리오 12건 | +1.40℃ | +9.21℃ | **+7.81℃** | **13.57** | +0.97 |

원본은 +2.6℃ 차이인데 **보정 램프가 +5.6℃ 더 벌어진다.** 보정값은 근무 내내 단조로
증가하고 그 증가분의 2/3(+6.31℃)가 **주입이 시작되기도 전** 구간에서 생긴다.
`fit_noise()` 가 이상이 포함된 그 근무의 데이터로 외기 계수를 적합하기 때문으로 보인다.

시나리오 0건 근무는 **이벤트 0건**이다. 기준선 두께 문제도 아니다 — 깨끗한 6근무로
기준선을 채운 DB 에서도 결과가 같았다.

**정보 자체는 있는데 선택에서 버려진다.**

| 창(분) | 최강 창 시작 | k | r |
| ---: | --- | ---: | ---: |
| 60 | **04:15** | 3.66 | 0.826 |
| 120 | 04:00 | 5.10 | 0.876 |
| 240 | 02:00 | 5.10 | 0.823 |
| 720 | 18:00 | **13.57** | 0.975 |

60분 창이 **04:15** 에 잡는다 — 실제 주입 시작 04:17 과 2분 차이다. 그런데 선택 규칙이
`k >= strongest * 0.6` 이라 문턱이 8.14 가 되고 짧은 창이 전부 탈락한다.

「문턱을 넘은 창 중 가장 좁은 것」 으로 바꿔 재봤더니 `#9` 는 해소되지만 `#2`(12시간 창에서만
보이는 종)와 `#19`(원복 구간을 집음)가 나빠져 **IoU 평균이 0.71 → 0.68 로 내려갔다.**
남는 교환이 아니라 **커밋하지 않고 되돌렸다.** 판단은 `#24` 에 넘겼다.

> `#23` 감도 스윕의 비단조도 같은 뿌리일 수 있다 — 같은 태그(`TI-403`)이고, 진폭을 줄이면
> 외기 오염 정도도 같이 줄어드니 창별 k 가 뒤바뀔 수 있다. 가설이다.

> **IoU 는 조기 검출을 깎는다.** `#8` 은 25분 일찍 잡아서 좋은 건데 IoU 는 0.42 로 낮다.
> IoU 를 올리는 것 자체가 목표가 아니라, **포함률이 가리는 것을 보는 용도**로 쓴다.

### 남은 오탐 2건

```
2026-08-21-night  LI-806   레벨시프트   19:27~20:17
2026-08-21-night  AI-803   이탈        01:08~01:09
```

앞의 것은 `#10` 이 19:21 에 끝난 **직후 레벨이 원래대로 돌아오는 구간**이다. 주입이 만든
것인데 주입 구간 밖이라 오탐으로 세어진다. 채점에서 「회복 구간」을 주입에 붙일지는
판단이 필요하다 — `#22` 에 올려 뒀다.

## 규모와 경계 (2026-08-29 · 5차)

### 태그를 16배로 늘려도 검출은 108초다

「53태그로만 검증했으니 실제 공정(수백~수천 태그)에서는 못 쓴다」 를 실제로 재봤다.
태그 마스터를 복제해 늘리고(연결도 각 사본 안에서 다시 이어) 검출을 돌렸다.

| 태그 수 | 연결 쌍 | 검출 시간 | 태그당 |
| ---: | ---: | ---: | ---: |
| 53 | 29 | 2.8초 | 53.6ms |
| 106 | 58 | 6.2초 | 58.3ms |
| 212 | 116 | 13.6초 | 64.1ms |
| 424 | 232 | 32.7초 | 77.2ms |
| **848** | 464 | **108.3초** | 127.8ms |

**제곱이 아니다.** 태그 16배에 시간 39배 — 대략 `n^1.3` 이다. 선형이면 44.8초,
제곱이면 717초일 텐데 그 사이다.

이유는 **상관 검출기가 전수 조합을 안 하기 때문**이다. 태그 마스터의 `links` 에 적힌
쌍만 본다 — 53태그에서 전수 조합은 1,378쌍인데 실제로 보는 것은 **29쌍**이다.
태그가 늘어도 쌍은 링크 수에 비례해 선형으로 는다.

> **실용상 문제가 없다.** 검출은 근무당 한 번 도는 작업이고 목표가 「교대 60분 전 생성」
> 이라, 848태그에서 2분은 여유다.
>
> **다만 메모리가 진짜 제약이다.** 848태그 × 21,600점 × 8바이트 ≈ 146MB 에 블록·뷰가
> 얹힌다. 서버 `MemoryMax` 가 700MB 라 그쯤에서 걸린다. 확장할 때 늘려야 할 것은
> CPU 가 아니라 메모리다.

### 시각 경계 12건 전부 정상

| 시각 | 배정 | |
| --- | --- | --- |
| 05:59:59 / 06:00:00 | 전날 야간 / 당일 주간 | ✅ |
| 17:59:59 / 18:00:00 | 주간 / 야간 | ✅ |
| 자정 직전 / 자정 | 같은 야간 근무 | ✅ |
| 월말 야간 → 다음 달 새벽 | 한 근무로 유지 | ✅ |
| 연말 야간 → 다음 해 새벽 | 한 근무로 유지 | ✅ |
| 2월 말 · 윤일(2028-02-29) | 정상 | ✅ |

모든 경우에 근무 창이 정확히 12시간이고 해당 시각이 그 창 안에 든다.

## 정답지 자체를 파형과 대조했다 (2026-08-29 · 4차)

지금까지는 정답지를 자로 삼아 엔진을 쟀다. **그 자가 맞는지**를 확인한 적은 없었다.
시나리오를 하나도 안 넣은 같은 seed·같은 시각의 근무를 만들어, 정답지가 「영향 태그」 라고
적은 태그가 **주입 구간에서 실제로 움직이는지** 하나씩 대조했다.

### 24종 중 2종이 실제보다 넓게 적고 있었다

| 시나리오 | 선언 | 실제로 움직임 | 무반응 |
| --- | ---: | ---: | --- |
| `#3` 개도 반복변동 | 7개 | 3개 | `LI-701` `FI-710` `PDI-702` `TI-704` |
| `#11` 개도–유량 상관 붕괴 | 7개 | 3개 | 〃 |
| 나머지 22종 | — | 전부 일치 | — |

**원인은 전파 단계다.** `applyLinks` 는 **1단계만** 전파한다 — `add[]` 를 원본 `D` 에서
계산해 한 번에 더하므로, 2단계는 닿지 않는다.

```
ZI-601 ──links──▶ FI-602, PI-604            ← 여기까지 간다
FI-602 ──links──▶ FI-710, LI-701, PDI-702, TI-704   ← 여기는 안 간다
```

물리적으로는 개도가 흔들리면 유량이 흔들리고 그것이 하류로 가는 게 맞다. **생성기가 덜
전파하는 것**이고, 정답지는 물리를 보고 적었다. 다만 **채점은 데이터로 하므로** 정답지를
데이터에 맞춘다 — 영향 태그가 넓으면 **그 태그에서 나온 아무 이벤트나 탐지로 세어져
채점이 관대해진다.**

### 점수를 부풀리고 있었나 — 아니다

`#3`·`#11` 이 어떤 태그로 탐지됐는지 확인했다. **둘 다 살아 있는 태그로 잡혔다**
(`ZI-601`·`FI-602`·`PI-604`). 무반응 태그를 빼도 탐지가 유지된다.

정답지를 좁히고 재채점한 결과도 **한 자리도 바뀌지 않았다.**

| | 좁히기 전 | 좁힌 뒤 |
| --- | --- | --- |
| `data/` 2근무 | 22/22 · IoU 0.71 · 오탐 2 | **동일** |
| `data/sim` 6근무 | 46/46 · IoU 0.68 · 오탐 7 | **동일** |

> **다단계 전파는 마감 후 과제로 둔다.** 고치면 물리적으로 더 맞지만 모든 파형이 바뀌어
> 지금까지의 측정을 전부 다시 해야 한다. 지금은 정답지를 데이터에 맞춰 두고,
> 문서에 「생성기는 1단계만 전파한다」 를 명시한다.

## 변칙 재검증 (2026-08-29 · 지금까지 시험을 조건 바꿔 다시)

### ⚠ 정정 — 「정답지 60분 옮겨도 100%」는 착시였다

적대적 시험 B 의 이 결론은 **`LI-806` 결함이 만든 것**이었다. 근무 전체에 걸린 이벤트가
무엇과도 겹쳐 주니 정답지를 옮겨도 맞는 것처럼 보였다. 고친 데이터로 다시 재면 무너진다.

| 정답지 이동 | 포함률 | IoU 평균 | IoU≥0.5 |
| --- | ---: | ---: | ---: |
| +0분 | **100%** | 0.71 | 17/22 |
| +15분 | 91% | 0.56 | 15/20 |
| +30분 | 91% | 0.39 | 8/20 |
| +60분 | **75%** | 0.30 | 5/15 |
| +120분 | 37% | 0.26 | 1/7 |
| +240분 | 11% | 0.29 | 0/2 |

**채점이 관대하다는 지적은 이제 유효하지 않다.** IoU 가 포함률보다 먼저·더 가파르게
떨어지는 것도 보인다 — 병기 판단이 실측으로 뒷받침된다.

### 새 결함 2건 (둘 다 수정함)

- **`level_shift` 오프바이원** — 가드가 `n < 2*w` 인데 탐색이 `range(w, n-w, ...)` 라
  `n` 이 정확히 `2w`(40블록)면 `best` 가 `None` 인 채 언팩돼 `run` 이 TypeError 로 죽었다.
  `n <= 2*w` 로 고쳤다. `n > 2w` 는 동작이 완전히 같고, `data/` 2근무 회귀 대조에서
  이벤트·포함률·IoU·오탐이 한 자리도 안 바뀌었다
- **시간대 붙은 시각** — `...T06:00:00+09:00` 이 오면 근무 배정에서 offset-naive 와
  비교하다 TypeError. 조용히 `tzinfo` 를 떼면 UTC 표기를 현지시각으로 오인해 9시간 밀린
  근무에 넣게 되므로, **추측하지 않고 이유를 밝혀 거부**하도록 했다

### 새 소견 — 값의 형식은 보는데 타당성은 안 본다

값 변칙 19종 중 13종은 걸렸는데 **6건이 조용히 저장**됐다. `TI-403` 은 정상범위
−178~−168℃ 인 극저온 태그다.

| 원문 | 저장된 값 | 왜 통과했나 |
| --- | ---: | --- |
| `1_000.0` | **1000.0** | 파이썬 언더스코어 숫자 리터럴을 `float()` 이 받는다 |
| `1,234.5` | **1.0** | 따옴표 없는 쉼표라 CSV 열이 쪼개지고 `1` 만 남는다 |
| `٣.٥` | 3.5 | `float()` 이 아랍-인도 숫자를 받는다 |
| `+.5` / `-0.0` | 0.5 / 0.0 | 유효한 실수 표기 |
| `1e-400` | 0.0 | 언더플로 — 조용히 0 이 된다 |

> **물리범위 검사는 넣으면 안 된다.** `#12` 분석기 순간 이상값이 `MI-804`(정상 0.1~1.5 ppm)에
> **999 ppm 을 일부러 주입**한다. 범위로 막으면 정상 시나리오가 거부된다.

### 다시 확인해 문제 없던 것

| 시험 | 변칙 | 결과 |
| --- | --- | --- |
| 정상 데이터 오탐 | 6근무 → **14근무 전량** | **14/14 이벤트 0건 · 초안 0항목** |
| 파일 구조 | BOM · CRLF · 열 순서 · 여분 열 · 값 따옴표 · cp949 | 전부 정상 적재 |
| 헤더 없음 | — | traceback 없이 「열이 없습니다」 안내 |
| 멱등성 | 같은 파일 2회 적재 | 595행 그대로 · 중복 0 |
| 합성 티(첨도) | 재생성 데이터로 재측정 | 1.91 ~ 2.43 (옛 1.95 ~ 2.48 과 같음) |
| 중복 행 | 600행 중 300행 중복 | 300행만 저장 · 충족률 절반 — 기존 소견 재확인 |

## `data/sim` 6근무 실측 (2026-08-29 · A안 수정 후 재생성분)

제출용 2근무보다 분모가 큰 쪽이다. 랜덤 6~10종을 넣은 6근무를 깨끗한 기준선 위에 올려 쟀다.

```
근무                      이벤트    주입    탐지    오탐
2026-08-22-day           27     7     7     1
2026-08-22-night         32    10     9     2
2026-08-23-day           31     9     9     1
2026-08-23-night         30     7     7     0
2026-08-24-day           30     8     8     2
2026-08-24-night         25     6     6     1
------------------------------------------------------------------
주입 건 포함률   46/46  (100%)
시나리오 종 커버  21/21 종
시각 일치도 IoU  전체 46건 평균 0.68 · 중앙 0.74 · IoU≥0.5 35/46
  ├ 국소형 37건  평균 0.69 · 중앙 0.77 · IoU≥0.5 28/37
  └ 추세형  9건  평균 0.65 · 중앙 0.59 · IoU≥0.5  7/9
오탐             7건 / 이벤트 175건  (근무당 1.2건)
알람 선행 검출    16/18 건이 알람선 도달보다 먼저  (최대 +666분)
```

### 오탐 7건은 전부 「복귀 구간」이었다

3차 적대적 시험에서 오탐 하나하나의 정체를 확인했다. **7건 전부가 주입이 끝나고 값이
원래대로 돌아오는 구간**이었다.

| 근무 | 태그 | 종류 | 구간 | 주입 종료와의 간격 |
| --- | --- | --- | --- | ---: |
| 08-22-day | `AI-803` | 이탈 | 14:50~14:51 | `#15` 종료 **2분** 뒤 |
| 08-22-night | `FI-204` | 레벨시프트 | 22:32~23:22 | `#8` 종료 **8분** 뒤 |
| 08-22-night | `TI-301` | 레벨시프트 | 01:17~02:07 | `#16` 종료 **7분** 뒤 |
| 08-23-day | `AI-803` | 이탈 | 09:59~10:00 | `#15` 종료 **2분** 뒤 |
| 08-24-day | `AI-803` | 이탈 | 15:42~15:43 | `#15` 종료 **2분** 뒤 |
| 08-24-day | `AI-707` | 레벨시프트 | 15:47~16:37 | `#15` 종료 **8분** 뒤 |
| 08-24-night | `TI-301` | 레벨시프트 | 23:25~00:15 | `#16` 종료 **0분** 뒤 |

**시나리오를 하나도 안 넣은 14근무에서는 이벤트가 0건**이다. 즉 이 7건은 베이스라인이
만든 것이 아니라 **주입이 만든 것**이고, 엔진은 정확히 보고했다. 정답지가
「주입 시작~종료」 만 구간으로 적는데 **실제 신호는 복귀까지가 한 사건**이라 생기는 차이다.

`tools/score.py` 가 이제 오탐을 두 줄로 낸다 — 전체 수는 그대로 내고 그중 복귀가 몇 건인지를
함께 낸다. **오탐을 줄여 보이려는 것이 아니라 성격을 나눠 보여주려는 것**이다.

```
오탐             7건 / 이벤트 175건  (근무당 1.2건)
  └ 그중 7건이 주입 종료 직후 복귀 — 복귀를 빼면 0건 (근무당 0.0건)
```

제출용 `data/` 2근무도 같다 — **오탐 2건이 전부 복귀**(`#10` `LI-806`, `#15` `AI-803`)라
빼면 0건이다.

> **엔진이 잘못 잡은 것은 지금까지 한 건도 없다.** 오탐으로 집계된 것은 전부
> ① 우리 데이터 결함(`LI-806`, 고침) 이거나 ② 정답지 구간 정의 문제(복귀)였다.

### 초안이 이벤트를 버리지 않는다

이벤트 175건이 초안 44항목이 되므로 「4분의 3이 사라지는가」를 확인했다.
**근거 본문까지 훑어보니 175건 전부가 초안 어딘가에 실려 있었다 — 누락 0건.**
`event_id` 로 직접 붙는 것은 44건이고 나머지는 **묶음 항목의 근거로 들어간다**(제목의
「외 3개 태그」). HH/LL 초과가 초안에서 빠진 경우도 **0건**이다.

### 채점 결함 하나를 고쳤다 — 이월 꼬리

처음 채점에서 **46/47 (98%)** 로 미탐지 1건이 나왔다. 파고들어 보니 **엔진이 못 잡은 것이
아니라 채점이 잘못 센 것**이었다.

| | |
| --- | --- |
| 「놓친」 주입 | `#1` 순도 헌팅 · `2026-08-22-night` · **18:00:00 ~ 18:00:38 (38초)** |
| 실제로는 | 앞 근무(주간)에 `17:30:38~18:00` 로 주입됐고 **`17:31~17:42` 헌팅으로 검출됨** |
| 왜 못 잡나 | 엔진은 30초 블록이 **최소 20개(10분)** 있어야 판정한다. 38초는 1~2블록이라 원리상 대상이 아니다 |

`continues_next`(근무 끝까지 이어지는 주입)를 「추적 중」 으로 빼는 규칙은 이미 있었는데,
**그 반대쪽인 `carried_in` 꼬리는 빠져 있었다.** 앞 근무에서 이미 판정된 건을 다음 근무에서
다시 세면서 못 잡았다고 집계한 것이다. 같은 이유로 분모에서 뺀다 —
경모님이 `continues_next` 에 대해 하신 말 그대로다: **"실제로 검출 못 해도 놓침이라 뜨면 오답"**.

`tools/score.py` 에 `MIN_DETECTABLE_SEC = 600` 을 두고 `carried_in` + 600초 미만이면
**「이월 꼬리」** 로 분류해 분모에서 뺀다. 화면에도 그렇게 표시한다.

> 20초짜리 `#14`(터빈 회전수 순간 강하)처럼 **원래 짧은 시나리오는 그대로 센다** —
> `carried_in` 이 아니기 때문이다. 실제로 검출도 된다.

## 결함 데이터 시험 (2026-08-29 · 03 「조건을 바꿔가며 시험」)

기준선 근무 하나(08-20 야간)에 오염률 **0.2%** 로 세 종류를 만들었다 — 1,144,800행 중 **2,223행**.
값은 생성기 `BADV` 그대로(`N/A · NaN · --- · ERR · #VALUE! · null · 1.2.3`).

| 시험 | 빈 값 | 깨진 값 | 혼합 |
| --- | --- | --- | --- |
| 기본 적재(플래그 없음) | ✅ 첫 행에서 멈춤 | ✅ 멈춤 | ✅ 멈춤 |
| `--skip-bad-rows` | ✅ 2,223행 건너뜀 · **1,142,577점 적재 · 충족률 99.8%** | ❌ **크래시** | ❌ **크래시** |

**잘 되지 않은 조건** — `--skip-bad-rows` 가 `NaN` 을 걸러내지 못해 적재가
`sqlite3.IntegrityError: NOT NULL constraint failed` 로 죽는다. 원인은 **`float('NaN')` 이
예외를 던지지 않는 것**이다(BADV 7종 중 `NaN` 만 통과). 나머지 6종과 빈 값은 정상적으로 걸러진다.
`app/collect.py` 담당인 박경모님께 **`#20`** 으로 올렸고, 고치면 같은 세 파일로 재시험해
「고친 뒤」 결과를 여기에 덧붙인다.

실제 DCS 내보내기에서 `NaN` 은 계기 통신 두절·연산 오류·분석기 무효값으로 흔히 나오므로,
`N/A` 나 `---` 보다 오히려 자주 만나는 값이다.

## 반복 시험 — 같은 이상을 5근무 연속 (2026-08-29)

근무자가 **매번 같은 항목을 마주할 때** 시스템이 어떻게 구는지 봤다.
`#10 LIN 레벨 임계 근접` 을 5근무 연속 주입하고 배경 이상은 근무마다 바꿔
「같은 문제 · 다른 상황」이 되게 했다. 근무자 역할로 **1·2회차는 코멘트를 달아 확정하고,
3회차부터는 일부러 계속 제외**했다.

| 회차 | 근무자가 한 일 | 과거 조치 | 초안에 올라왔나 |
| --- | --- | --- | --- |
| 1회 | 채택 + 코멘트 | 0건 | 올라옴 |
| 2회 | 채택 + 코멘트 | **1건** | 올라옴 |
| 3회 | **제외** | **2건** | 올라옴 |
| 4회 | **제외** | 2건 | 올라옴 |
| 5회 | **제외** | 2건 | 올라옴 |

**① 놓치지 않는다** — 5근무 전부 초안에 올라왔다.

**② 순환이 실제로 돈다** — 1회차에 남긴 문장이 2회차 초안의 「과거 조치」로 **출처 근무와 함께**
돌아왔고, 2회차 것이 더해져 3회차부터 2건이 쌓였다. 그리고 **제외한 회차의 코멘트는 쌓이지
않는다** — 3·4·5회차 내내 2건에서 멈춰 있다. *확정된 것만 회수한다* 는 설계가 정확히 동작한다.
기획서 3-5 의 근거로 그대로 쓸 수 있는 실측이다.

**③ 계속 제외해도 억제하지 않는다** — 세 번 연속 제외했는데 5회차에도 그대로 올라왔다.
**이것은 맞는 설계로 본다.** 「두 번 넘겼으니 세 번째부터 숨긴다」 는 위험하다 — 앞 근무자가
잘못 판단했을 수도, 그 사이 상황이 달라졌을 수도 있다. 다만 **몇 번째인지는 화면에 보이지
않는다** — 다섯 번째 항목이 첫 번째와 똑같이 생겼다. 「최근 5근무 중 5회 감지 · 그중 3회 제외됨」
같은 표시를 **`#21`** 로 제안해 두었다(결선 과제).

**DB 무결성** — 근무 5 · 이벤트 70 · 초안 19항목 · 확정 일지 5건. 5회차를 거치며 깨진 곳이 없었다.

> ⚠ **이 시험에는 전용 시나리오 10종을 따로 만들어 썼다** — 그동안 어느 시나리오의 주역도
> 아니던 계통(B탑 재생 · 터빈 · LP 컬럼 · GN2 벤트 · LIN 제품)을 채우는 이상들이다. 전부
> 검출됐고 알람 미발생으로 설계대로 동작했지만, **본 생성기(24종)에는 넣지 않았다.**
> 공식 성능 수치를 흔들지 않기 위해서다. 시험 전용 사본에만 존재한다.

## 5일 랜덤 시뮬레이션 실측 (2026-08-28 · 10근무 · `seed 20260901`)

정기영님 엔진(`engine/`)에 실제로 적재→검출→채점을 돌렸다. 랜덤 6~10종 × 10근무, 357.8MB.

| 항목 | 결과 |
| --- | --- |
| **주입 건 포함률** | **74/74 (100%)** — **미탐지 0건** |
| 시나리오 종 커버 | **22/22 종** (5일이면 전 종이 나온다) |
| 오탐 | **11건 / 이벤트 282건 · 근무당 1.1건** |
| 알람 선행 검출 | 24/29 건이 알람선 도달보다 먼저 · **최대 +653분** |
| 데이터 건전성 | HH/LL 침범 0건 · 금지조합 위반 0건 |

**오탐 11건의 원인은 두 가지뿐이고, 엔진 오작동은 0건이다.**

| 원인 | 건수 | 성격 |
| --- | --- | --- |
| `LI-806` 구조적 H 초과 | **6건** | **데이터 결함** — 아래 「알려진 결함」 그 건. 시나리오 없이 베이스라인만으로 H 를 넘어 근무 내내(512~716분) 보고된다. 엔진은 정확히 보고했다 |
| 주입 종료 직후 **복귀**를 잡음 | **5건** | **정답지 구간 정의 문제** — 정답지는 주입 시작~종료만 구간으로 잡는데 실제 신호는 복귀까지가 한 사건이다 |

복귀 5건은 모두 **레벨시프트 · 정확히 50분**이다(엔진 검출창 구조). 종료 후 2~10분에 잡혔다 —
`PDI-103`(#19 종료 2분 후) · `TI-401`(#4 종료 3분 후) · `TI-301`(#16 종료 3분 후) ·
`AI-707`·`AI-803`(#15 종료 3·10분 후). **복귀도 인계 가치가 있다** — 20번 시나리오의 설계 취지
(「알람 발생 후 해제, 적지 않으면 다음 근무자가 모름」)와 같은 상황이다. 정답지 구간을
종료 후 여유까지 확장하거나 채점기가 종료 직후 같은 태그 이벤트를 인정하도록 하면 사라진다.

**연동 전파 태그 누락을 고쳐 오탐이 13 → 11 건으로 줄었다.** 태그 마스터의 연동
(`AI-707→AI-803` gain 0.80 등)으로 따라 움직이는 태그가 정답지 `affected_tags` 에 빠져 있어
정상 전파가 오탐으로 집계됐다. 6개 시나리오(#1·#3·#6·#11·#23·#24)를 정정했고 파형은 바뀌지
않았다(CSV 해시 동일). 이제 연동 누락 0건이다.

**초안 항목은 이벤트보다 훨씬 적다** — 검출 이벤트 19~40건 → 초안 4~10개(약 4:1 압축).
파이프라인이 이벤트를 묶는다. 정지 시나리오의 「초안 39항목」 추정은 과대였다(아래 절에서 정정).

**운영 제약 하나** — 원본 보관이 3일(`RAW_RETENTION_DAYS = 3`)이라 10근무를 한꺼번에 적재하면
앞 4근무 원본이 지워진다(실측 4,579,147점 삭제 = 4근무 × 1,144,800). **근무마다 적재→검출**로
돌려야 한다. 실제 운영(교대마다 실행)과 같은 순서이므로 시뮬레이션으로도 이쪽이 맞다.

## ✅ 완화됨 — 플랜트 정지 시 초안 폭주 (08-28 실측 · 08-29 확인 질문 추가)

> **2026-08-29.** 정지로 보이면 초안 맨 앞에 **「플랜트를 정지하셨습니까?」** 확인 질문을
> 세운다. 시스템이 정지라고 단정해 묶지 않는다 — 계획 정지인지 Trip 인지 대규모 계측
> 장애인지는 사람만 알고, 틀리게 묶으면 되돌릴 수가 없다. **판단을 사람에게 넘기고 그 답을
> 일지에 남긴다.** 항목 수 자체는 그대로다(A안). 답에 따라 자동으로 접는 것은 B안으로 남겼다.

### 문턱을 어떻게 정했나

「근무 전체 태그의 30%」로 하면 **오작동한다** — 이상 12종을 넣은 **정상** 근무가 32.1% 라
걸린다. **10분 창 안에 동시에 시작된 태그 비율**로 보면 깨끗하게 갈린다.

| 근무 | 10분 창 동시 이탈 | 근무 전체 |
| --- | ---: | ---: |
| Trip 야간 | **90.6%** (48/53) | 90.6% |
| 계획정지 주간 | **69.8%** (37/53) | 86.8% |
| 이상 12종 주입 | **7.5%** (4/53) | 32.1% |

두 무리 사이가 **62%p** 벌어져 30% 문턱이 한가운데 놓인다.

### 실제 출력

```
[상] 플랜트를 정지하셨습니까? — 18:00 경 48개 태그가 동시에 이탈
     18:00 부터 10분 안에 태그 48개(전체의 91%)가 한꺼번에 정상 범위를 벗어났습니다.
     계획 정지·Trip·대규모 계측 장애 중 어느 것인지는 데이터만으로 판단할 수 없어 여쭙니다.
     가장 먼저 움직인 태그: AI-402, AI-703, AI-707, AI-803
     · 정지가 맞다면 — 아래 항목들은 정지에 따른 결과입니다. 「정지 사건」으로 한 번에
       처리하시고 정지 사유와 first-out 을 코멘트에 남겨 주십시오.
     · 정지가 아니라면 — 동시 다발 이탈이라 계측·통신 계통을 먼저 확인해 주십시오.
```

**검출 엔진은 건드리지 않았다.** 초안을 조립하는 `app/ports.py` 에만 얹었다.

**확인** — Trip 야간 뜸 · 계획정지 주간 뜸 · 이상 12종 주입 **안 뜸** · 깨끗한 근무 이벤트 0.
회귀 시험 4건을 `tests/` 에 넣어 문턱이 두 무리 사이에 남는지 계속 지킨다.

아래는 발견 당시의 기록으로 남긴다.

## ⚠ 알려진 한계 — 플랜트 정지 시 초안이 폭주한다 (2026-08-28 실측)

23·24 를 만든 이유의 절반은 **우리 시스템이 정지 상황을 못 견딘다는 것을 실측해 기록**하는 데 있습니다.
「03. 데이터 세트」의 확인 항목이 *조건을 바꿔가며 시험 + **잘 되지 않았던 조건도 적을 것***이므로,
이 한계 자체가 제출물입니다.

| 항목 | 정상 근무 | 플랜트 정지 (23·24) |
| --- | --- | --- |
| 이탈 태그 | 1~5점 | **44점** (외기 2점·탱크 레벨 제외) |
| 알람 발생 | 0~3건 | **39건** — 그중 **HH/LL(설비정지 수준) 도달 26건** |
| 초안 항목 | **4~10개** (5일 실측) | **약 10개 추정** — 이벤트 39건이 약 4:1 로 묶인다. 처음 「39항목」 이라 추정했으나 5일 실측에서 이벤트→초안 압축비를 확인해 정정했다 |
| AI 처리 시간 (`BATCH=5` 순차, 실측 16항목 = 5분 42초) | 약 6분 | **약 20분** |

**엔진에는 건수 상한이 없습니다.** `engine/`·`app/` 전체에 cap 코드가 없고, 유일한 억제는 알람 규칙의
「같은 방향에서 더 심한 것 하나만」(태그별 1건)뿐입니다. AI 의 `handover_worthy` 는 전달 가치가 없는
항목을 걸러내지만 **정지 알람은 전부 전달 가치가 있다고 판정**될 것이므로 여기서 줄지 않습니다.
AI 의 의미 묶음(`related_idx`)은 인과로 **묶어 표시만** 하고 항목은 분리 유지합니다(승인이 항목 단위).
결국 **교대 직전에 카드 39장을 검토해야 합니다 — 현실적으로 불가능합니다.**

**강제 주입 2근무 실측** (2026-08-28, `seed 20260829`)

| 근무 | 주입 | 알람 | first-out 선언 | first-out 실측 |
| --- | --- | --- | --- | --- |
| 08-29 주간 | #23 계획정지(07:00~13:00) + #21 LIN 출하 | 39건 (HH/LL 26) | AIR COMPRESSOR K-201 | **K-201 · 0초 ✓ 일치** |
| 08-29 야간 | #24 Trip(20:00~02:00) + #21 이어짐 + #22 LOX 출하 | 39건 (HH/LL 26) | AIR COMPRESSOR K-201 | **K-201 · 2초 ✓ 일치** |

**탱크 출하는 정지와 무관하게 정상 동작했습니다**(알람 미발생) — 정지 캐스케이드에서 탱크 계통을
제외한 설계가 의도대로 작동합니다. 실제로도 플랜트가 멈춰도 탱크에서 출하는 나갑니다.
금지조합 충돌 0건.

**대응 방향** (결선 과제로 제안, `#19` 에서 논의)

1. **first-out 을 앞세운다** — 원인 유닛이 정해지면 나머지 38건은 「후속」 한 줄로 접힌다. 엔진이 이미
   이벤트별 시각을 갖고 있으므로 **변화 시작 시각으로 정렬**만 하면 된다. 가장 값싸고 가장 유용하다.
2. **정지 감지 → 초안 템플릿 전환** — 판정 신호는 우리 태그로 만들 수 있다(`JI-206` 전력 급락 +
   주요 유량 급락 동시). 감지되면 항목 나열이 아니라 **사건 보고서 1건 + 접힌 알람 목록**.
3. **알람 폭주 억제** — 짧은 창(60초) 안에 N건 이상이면 하나의 정지 사건으로 접는다 (ISA-18.2 개념).
4. **계획 정지는 정상 작업으로** — 23 은 `operation:true` 다. 출하와 같은 취급.

**검증 기준 예외** — 23·24 는 **HH/LL 을 26건 넘습니다**(실측). 플랜트가 실제로 정지하는 시나리오이므로
검증 배터리의 「설비정지 수준 알람 0건」 기준을 이 두 종에는 적용하지 않습니다. 같은 이유로 정지 구간에서
유량·전력·회전수가 **0 에 고정되는 것도 정상**입니다(정지한 모터의 계기는 0.0 을 읽습니다).
`AI-803`(LOX 순도)이 정지에 따라 움직이는 것도 정상입니다 — `AI-707`(LP O2 순도)이 연동 대상으로
갖고 있어(gain 0.80 · 지연 180초) 컬럼 제품 순도가 탱크 순도를 끌고 갑니다.

## ✅ 해결된 결함 — 10번 시나리오의 전제가 무너져 있던 것 (08-28 발견 · 08-29 수정)

> **2026-08-29 A안으로 수정 완료.** `LI-806` 드리프트를 **3단 → 2단**으로 낮춰
> 베이스라인 최대가 **85.7%** 가 됐다(H 88 까지 여유 2.3). `data/`·`data/sim` 을 재생성했고
> **엔진 재측정이 필요하다**(`#18`). 아래는 발견 당시의 기록으로 남긴다.

`LI-806`(LIN 레벨)은 **시나리오를 하나도 주입하지 않은 베이스라인만으로도 H(88%)를 넘었습니다.**

| 확인 | 값 |
| --- | --- |
| 태그 설정 | nominal 80 · 정상범위 40~90 (폭 50) · H 88 · **드리프트 3단** |
| 드리프트 진폭 | 폭의 12% = **±6.0**, 보조 드리프트 0.4배 = ±2.4 → 합 **8.4** |
| 산술적 최대 | 80 + 8.4 = **88.4 > H 88** → **seed 와 무관하게 구조적** |
| 실측 (시나리오 0건, 기준선 14근무) | 최대 **89.3 ~ 89.9** — 14근무 **전부** 초과 |
| 실측 (기존 기준 2근무) | 주간 89.4 · 야간 89.2 |
| 노이즈 σ | 0.25 — 노이즈가 아니라 **드리프트가 원인** |

**무엇이 문제인가**

10번은 "상한 인근에 머물다 후반에 돌파해서 알람이 울리는" 시나리오입니다. 그런데 주입하지
않아도 이미 넘으므로, 엔진이 `LI-806` 의 H 초과를 보고해도 **그것이 10번을 잡은 것인지 상시
드리프트를 잡은 것인지 구분할 수 없습니다.** 정답지의 `dcs_alarm: 발생 · LI-806 H` 도 10번
때문이라고 단정할 수 없습니다.

**고치는 방법과 비용**

`LI-806` 의 드리프트를 **3단 → 2단**(폭의 6% = ±3.0, 보조 ±1.2 → 최대 84.2)으로 낮추면 H 까지
3.8 여유가 생겨 "베이스라인은 정상, 10번 주입 때만 초과" 가 성립합니다. 다만 태그 마스터를
바꾸면 **`data/`·`data/sim/` 의 결함 데이터와 정답지를 재생성**해야 하고, 그 위에서 나온
**엔진 측정치(포함률·오탐)도 재측정**해야 합니다. 마감이 가까우므로 **팀 결정 사항으로 올립니다.**

**왜 지금까지 안 보였나**

검증 스크립트가 **HH/LL 만 검사**했고 H/L 은 시나리오 구간 안에서만 봤습니다. 정상 구간의
H/L 침범을 아무도 보지 않았습니다. 기준선용 정상 데이터(#17)를 만들면서 전수 검사를 넣어
드러났습니다.

## A안 수정 결과 (2026-08-29)

`#18` 을 정기영님·박경모님과 협의해 **A안(고치기)** 으로 정했다. 태그 마스터에서 `LI-806` 의
드리프트 단계를 하나 낮춘 것이 전부다.

| | 수정 전 (3단) | 수정 후 (2단) |
| --- | --- | --- |
| 드리프트 진폭 | ±6.0 + 보조 ±2.4 = **±8.4** | ±3.0 + 보조 ±1.2 = **±4.2** |
| 산술 최대 | 80 + 8.4 = **88.4** > H 88 | 80 + 4.2 = **84.2** < H 88 |
| 베이스라인 실측 최대 | 89.4 ~ 89.9 (**상시 초과**) | **85.7** (여유 2.3) |

**확인한 것**

- 시나리오 0건 베이스라인 4개 구간에서 `LI-806` 이 **74.5 ~ 85.7%** — H·L 어느 쪽도 넘지 않는다
- **10번 시나리오는 의도대로 작동한다** — 5개 주입 시각 전부에서 최대 88.3 ~ 89.3% 로 H 를 넘긴다.
  이제 「베이스라인은 정상, 주입할 때만 초과」 가 성립한다
- **21번 LIN 출하는 영향 없다** — 알람 미발생 유지, 레벨 최저 49.9%(L 25 여유)
- `data/` 2근무와 `data/sim` 6근무를 **재생성**했다

### 곁딸린 수정 — 10번 시나리오가 드리프트 위상에 얹혀 있었다

드리프트를 낮추자 **10번 시나리오가 절반쯤 H 를 못 넘었다**(재생성한 6근무 중 한 근무는
`#10` 을 주입했는데도 최고 87.9%). 원인은 시나리오 식이 고정값을 기준으로 잔차를 빼는
구조라, 베이스라인이 내려가면 봉우리도 같이 내려갔기 때문이다.

```js
// 전 — 고정값 87.3 에서 잔차를 뺀다. 드리프트가 작아지면 봉우리도 작아진다
D['LI-806'][i] = 87.3 + (D['LI-806'][i]-87.3)*0.25 + 1.7*ramp;

// 후 — 주입 직전 값을 기준으로 잡는다. 위상과 무관하게 87.3 에서 출발한다
if(c.memo.b===undefined) c.memo.b = D['LI-806'][s];
D['LI-806'][i] = 87.3 + (D['LI-806'][i]-c.memo.b)*0.25 + 2.2*ramp;
```

주입 위치를 1~9시로 옮겨가며 강제 주입해 확인했다. 봉우리 **90.2 ~ 91.8%**(HH 95 미만),
같은 조건의 베이스라인 최고 **85.1 ~ 85.6%**. 위상이 어디든 H 를 넘고, 안 넣으면 안 넘는다.

**재측정 결과 (같은 날 실측)**

- **정상 데이터 오탐 0 건** — 깨끗한 기준선 6근무에서 이벤트 0건 · 초안 0항목.
  수정 전 근무당 1.00건에서 **0** 이 됐다
- **10번 탐지가 진짜가 됐다** — 재생성한 `data/asu_shift_day_12h.csv` 에서
  「임계근접」 이벤트 `17:21:30 ~ 18:00:00`, 정답 구간 `17:21:40 ~ 18:00:00` → **IoU 0.996**.
  수정 전 0.03 ~ 0.21 의 가짜 탐지와 대비된다
- 8근무(`data/` 2 + `data/sim` 6) 전수 확인 — `#10` 을 넣은 근무만 H 를 넘고, 안 넣은
  근무는 최고 85.3 ~ 85.6% 로 넘지 않는다. **불일치 0건**

**남은 일** — 포함률 공식 수치를 새 데이터로 다시 산출(정기영님). 기준선 14근무는
드리프트 3단 시절 데이터라 재생성 여부 확인 필요(박경모님).

## 전파 경로와 기대 탐지 결과

위 표가 "무슨 일이 일어나는가"라면, 이 표는 **"그래서 어느 태그에서 몇 분 안에 잡혀야 하는가"** 입니다.
통합테스트 판정 기준으로 그대로 사용합니다. 기대대로 안 잡히면 알고리즘 쪽 문제, 엉뚱한 게 잡히면 오탐입니다.

| # | 트리거 태그 | 주입 파형 | 전파 (지연) | 기대 탐지 태그 | 탐지 목표 | DCS 알람 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `AI-707` | 감쇠 진동, **하향 편향** (−0.42%p) | `AI-803` (180초) | `AI-707` | 10분 이내 | 미발생 (L 99.0% 대비 최소 0.16%p 여유) |
| 2 | `TI-205` | 램프 (+0.68℃/h · 24시간) | 근무 경계 넘김 | `TI-205` | 60분 이내 | 외기에 따라 다름 (답안지 실측) |
| 3 | `ZI-601` | 펄스 × 3, 진폭 점증 | `FI-602` (즉시), `PI-604` (20초) | `ZI-601` **진폭 추세** | 2회차까지 | 미발생 |
| 4 | `TI-401` | 완만한 플래토 | — | `TI-401` | 20분 이내 | 미발생 |
| 5 | `FI-602` | 딥 (−5%, 10초) 반복 | `PDI-702` (20초), `TI-704` (30초), `FI-710` (60초) | `FI-602` + 동반 흔들림 | 3회 발생 내 | 미발생 |
| 6 | `MI-302` | 지수 상승 | `PDI-303` (선행 5분) | `PDI-303` → `MI-302` | **알람보다 먼저** (60분 이내) | **발생** (H 3ppm 초과) |
| 7 | `ZI-801` | 값 고정 (stuck, 2시간) | `LI-802` 추세 이탈 (60초) | `LI-802` | 60분 이내 | 미발생 |
| 8 | `PI-203` | 램프 + `FI-204` 역방향 램프 | `JI-206` (즉시) | `PI-203` × `FI-204` 조합 | 15분 이내 | 미발생 |
| 9 | `TI-403` | 램프 (+1.5℃/h) | `AI-707` (30분) | `TI-403` | 45분 이내 | 미발생 |
| 10 | `LI-806` | 플래토 87.3% → 후반 89.0% 돌파 | — | `LI-806` | **알람보다 먼저** (30분 이내) | ⚠ **판정 불가** — 베이스라인만으로도 H 초과 |
| 11 | `ZI-601`↔`FI-602` | 연동 이득 0으로 | `PI-604` 반응 소실 | 두 태그 **상관계수** | 40분 이내 | 미발생 |
| 12 | `MI-804` | 스파이크 (999ppm, 3초) | — | `MI-804` 단일 시점 이탈 | 발생 즉시 | 미발생 (on-delay 미달) |
| 13 | `ZI-904` | 진동 × 5 (±5%) | `PI-901` (30초), `FI-906` | `ZI-904` 발생 **횟수** | 3회차까지 | 미발생 |
| 14 | `SI-507` | 스파이크 (−800rpm, 20초) | `PI-504` (즉시) | `SI-507` | 발생 즉시 | 미발생 (지속 부족) |
| 15 | `AI-707` | 램프 (−0.3%p) | `AI-803` (180초) | 두 태그 **동반 저하** | 50분 이내 | 미발생 |
| 16 | `TI-301` | 램프 후 유지 (+22.9℃) | — | `TI-301` | **알람보다 먼저** (20분 이내) | **발생** (H 50℃ 초과) |
| 17 | `PI-901` | 램프 후 유지 (−0.50 bar) | — | `PI-901` | **알람보다 먼저** (30분 이내) | **발생** (L 4.50 bar 미만) |
| 18 | `LI-701` | 램프 후 유지 (+18.6%) | — | `LI-701` | **알람보다 먼저** (45분 이내) | **발생** (H 40% 초과) |
| 19 | `PDI-103` | 완만한 램프 (+0.021 bar) | — | `PDI-103` | **알람보다 먼저** (90분 이내) | **발생** (H 0.030 bar 초과) |
| 20 | `JI-206` | 펄스 (+430 kW · 10분 유지 후 원복) | — | `JI-206` | 유지 구간 내 (10분) | **발생 후 해제** (H 3,600 kW) |
| **21** | `ZI-805` | 개도 사다리꼴 (+29% · 2시간 유지) | `LI-806` **−27~40%p** (연동 gain −0.60 · 지연 60초) | `ZI-805`·`LI-806` | 레벨 시프트로 검출 | **미발생** — 계획된 출하 |
| **22** | `ZI-801` | 개도 사다리꼴 (+24% · 1.5시간 유지) | `LI-802` **−21~28%p** (연동 gain −0.60 · 지연 60초) | `ZI-801`·`LI-802` | 레벨 시프트로 검출 | **미발생** — 계획된 출하 |
| **23** | `JI-206`·`ZI-201` | 3시간 단계 감부하 | 유닛 순서대로 44점 | 43점 (탱크 순도 포함) | 유닛별 first-out 순서 | **다수 발생** (39건) |
| **24** | `JI-206` | 전력 상실 → 30초 붕괴 | 유닛 순서대로 44점 | 43점 (탱크 순도 포함) | 압축기 2초 → GN2 604초 | **다수 발생** (39건, HH/LL 포함) |

> **21·22 는 레벨을 직접 쓰지 않습니다.** 출하 밸브 개도만 올리고, 레벨 하강은 태그 마스터에
> 이미 있는 연동이 만듭니다. 그래서 물리 관계가 어긋나지 않고, 엔진에게는 **개도와 레벨이 함께
> 움직이는 전파 경로**로 보입니다.
>
> **출하 판정 규칙** — 레벨 태그가 **20%p 이상 감소하는 트렌드**면 출하로 봅니다. 두 시나리오
> 모두 어느 시각에 주입되든 이 문턱을 넘도록 만들었습니다(실측 최소 21.6%p). 정답지에는
> `operation: true` 로 기록되어 이상과 구분됩니다.

**알람 미발생 13종 + 알람 발생 7종 + 계획된 정상 운전 작업 3종 + 플랜트 정지 1종**으로 설계했습니다. 네 경로를 모두 시험하기 위한 것입니다.

| 구분 | 번호 | 목적 |
| --- | --- | --- |
| 알람 미발생 12종 | 1, 3~5, 7~9, 11~15 | ENGRA의 핵심 가치. 알람에 안 걸리고 조용히 지나가는 신호를 잡아내는지 |
| 알람 미발생 · **조건부 1종** | 2 | 근무를 넘는 드리프트. 끝나는 시각의 외기에 따라 알람 도달 여부가 갈림 |
| 알람 발생 · 복합 2종 | 6, 10 | 연동 기여가 더해져 **결과적으로** 한계를 넘은 경우 |
| 알람 발생 · 단순 5종 | 16~20 | 단일 태그가 한계를 명확히 넘는 평범한 건 (Issue #1) |

> **13/7 프레임과 실제 내역** — 설계 기준은 미발생 13 · 발생 7이고, 그중 `#2`는 **조건부**입니다. 새벽에 끝나면 외기가 램프를 상쇄해 미발생, 오후에 끝나면 발생입니다. 그래서 답안지의 알람 여부는 선언값이 아니라 **생성된 데이터에서 실측**해 기록합니다.
>
> 이 조건부 1종이 오히려 **ENGRA가 풀려는 문제의 원형**입니다 — 앞 근무에서 시작된 변화가 그 근무엔 알람을 울리지 않고, 다음 근무에서 한계에 닿습니다. 앞 근무자가 적지 않으면 다음 근무자는 원인을 모릅니다.

**알람이 울린 건도 인수인계장에 들어가야 합니다.** 근무자가 이미 아는 사건이라도 적는 걸 잊으면 똑같이 누락입니다. 그리고 알람 발생 7종은 모두 **알람이 울리기 한참 전부터 징후가 보이므로**, 판정 기준을 "알람보다 먼저 잡아내는가"로 잡았습니다.

6·10번과 16~20번의 차이가 중요합니다. 6·10번은 시나리오 자체는 한계 안에 있었는데 **연동 태그 기여가 더해져** 넘은 경우로, 실제 공정에서 이상이 커지는 방식입니다. 16~20번은 처음부터 단일 태그가 곧장 넘습니다. 앞은 "왜 넘었는지 설명할 수 있는가"를, 뒤는 "빠짐없이 잡는가"를 봅니다.

탐지 알고리즘 대응 (정기영님 `engine/` 참고용)

| 알고리즘 | 대응 시나리오 |
| --- | --- |
| Modified z-score (median/MAD) | 1, 4, 10, 12, 14, 16, 17, 18, 20 — 단일 시점 이탈 |
| CUSUM / EWMA | 2, 6, 9, 15, 19 — 누적 추세 이탈 |
| 이동 표준편차 · 진폭 추세 | 3, 5, 13 — 진동 크기와 반복 횟수 |
| 태그 간 상관 추적 | 5, 8, 11, 15 — 조합으로만 보이는 신호 |
| 값 정체 감시 (stuck) | 7 — 변화량이 0에 가까운 상태 지속 |
| 한계 접근 추세 (알람 예측) | 16~20 — 한계 도달 **전에** 추세로 먼저 잡기 |

> **전처리 주의 (12번)** — 999 ppm 같은 이상값이 그대로 통계에 들어가면 median/MAD와 이동 표준편차를 오염시켜 **다른 시나리오의 탐지까지 망가집니다.** 스파이크는 탐지 대상으로 기록하되 기준선 계산에서는 제외해야 합니다. 통합테스트에서 반드시 확인할 항목입니다.

---

## 채우면서 함께 봐 주실 것

- 이 유형은 **DCS 알람으로 잡히는가, 안 잡히는가?** — ENGRA의 핵심 가치는 알람에 안 걸리는 신호를 잡는 것입니다.
- 실제로 인수인계에 **적을 만한 가치가 있는가?** — 감지는 되는데 아무도 안 적는 항목이면 오탐입니다.
- 같은 현상을 두고 **근무자마다 판단이 갈릴 만한가?** — 갈린다면 그 자체가 우리가 해결하려는 문제의 사례입니다.

---

## 조치 문구 — 모의 과거 일지 시드

ENGRA에는 **과거 조치 추천** 기능이 있습니다. 같은 태그에 유사 증상이 잡히면, 과거 확정 일지에서 그때의 조치를 찾아 초안에 함께 제시합니다. 화면의 파란 칸이 그것입니다.

```
1근무   이상 감지 → 초안 항목 생성
        근무자가 승인하며 조치 코멘트 입력
                 ↓ 확정 일지에 저장
2근무   같은 태그에 유사 증상 감지
        → 초안에 파란 칸으로 뜸
          "과거 조치 추천 — 2026-06-12 야간조: (그때의 조치 문구)"
```

**문제는 시연 첫 화면에 과거가 없다는 것입니다.** 처음 돌리면 확정 일지가 비어 있어 파란 칸이 나오지 않습니다. 아래 문구로 **모의 과거 확정 일지**를 만들어 심어두면, 첫 화면부터 파란 칸이 채워집니다.

용도는 그것 하나입니다. **실제 운영에서 조치는 근무자가 승인 화면에서 직접 입력하며, 미리 정해둘 성질이 아닙니다** (Issue #3).

- **조치 문구** — 그 근무에서 실제로 한 일. 다음 근무 초안의 파란 칸에 이 문장이 뜹니다
- **인계 문구** — 다음 근무자가 무엇을 봐야 하는지

### 플랜트 정지 2종 — 멘트를 first-out 기준으로 쓴다

| # | first-out | 조치 문구 (그 근무에서 한 일) | 인계 문구 (다음 근무자가 볼 것) |
| --- | --- | --- | --- |
| 23 | AIR COMPRESSOR K-201 (0초, 계획 감부하) | 계획 정지 시행 — 압축기 K-201 감부하 3단 후 정지. 유닛 순서대로 정지 확인(콜드박스 → 터빈 → 컬럼 → 흡착탑 → 주열교환기 → GN2). 탱크 보압 유지 | **계획 정지 상태.** 알람 39건은 계획 정지의 정상 후속이며 개별 조치 불필요. 재기동 예정 시각 확인하고 콜드박스 승온 상태만 감시 |
| 24 | AIR COMPRESSOR K-201 (2초, 전력 상실) | **Trip 발생. 최초 이탈 압축기 K-201 소비전력(+2초)** → 콜드박스 인입(+8초) → 터빈(+14초) → 컬럼(+28초) → 흡착탑(+38초) 연쇄. 압축기 전원반 확인 요청, 콜드박스 승온 감시 중 | **원인은 압축기 K-201 이며 아직 규명 전.** 알람 39건은 전부 압축기 정지의 후속이고 개별 설비 이상이 아니다. 재기동 전 LOX/LIN 탱크 보압과 콜드박스 승온 상태를 먼저 확인 |

> **왜 first-out 을 앞세우나** — 정지 인계에서 근무자가 알아야 하는 것은 알람 39건의 목록이 아니라
> **어느 Unit 이 먼저 나갔는가**입니다. 원인 유닛이 정해지면 나머지 38건은 「후속」으로 한 줄에 접힙니다.
> 그래서 정답지에 `first_out` 필드를 넣어 **초안이 원인 유닛을 맞혔는지 채점**할 수 있게 했습니다.
>
> ⚠ **알람 순서와 원인 유닛은 다릅니다.** 알람은 「한계선까지의 거리」에 좌우되므로, 한계가 가까운
> 태그가 먼저 울립니다. 실측에서도 알람만 보면 콜드박스가 먼저 뜨는 구간이 있었습니다.
> **변화 시작 시각**으로 판정해야 압축기가 나옵니다. 이것이 이 두 시나리오의 시험 포인트입니다.

### 계획된 정상 운전 작업 2종

| # | 태그 | 조치 문구 (그 근무에서 한 일) | 인계 문구 (다음 근무자가 볼 것) |
| --- | --- | --- | --- |
| 21 | `LI-806` | LIN 출하 진행 — 탱크로리 3대, LV-802 개도 68% 로 2시간. 출하 후 재충전 정상 | LIN 레벨 46% 에서 재충전 중. 출하 종료 확인했고 다음 출하 스케줄 없음 |
| 22 | `LI-802` | LOX 출하 진행 — LV-801 개도 70% 로 1.5시간. 레벨 30% 에서 출하 중단(L 25% 접근 회피) | LOX 레벨 30% 에서 재충전 중. 출하 중단 사유는 L 접근이며 설비 이상 아님 |

> 이 두 건은 **이상이 아닙니다.** 그런데 인계에서 빠지면 다음 근무자는 레벨이 왜 낮은지 모릅니다.
> 엔진이 레벨 하강을 잡고 → 근무자가 「출하 진행」으로 확인해 확정하면 → 다음에 같은 패턴이 나올 때
> **사례 검색(태그 키)이 그 조치를 회수**합니다. 쓸수록 쌓이는 순환을 실증하는 재료입니다.

### 알람 미발생 13종

| # | 대상 | 조치 문구 | 인계 문구 |
| --- | --- | --- | --- |
| 1 | `AI-707` 순도 헌팅 | LP 컬럼 리플럭스 미세 조정, 30분 후 안정 확인 | 재발 시 순도 분석기 점검 요청 |
| 2 | `TI-205` 토출온도 드리프트 | 인터쿨러 냉각수 유량 확인 정상, 별도 조치 없이 추이 감시 | 앞 근무부터 시간당 0.7℃씩 상승 중. 125℃ 접근 시 부하 조정 필요 |
| 3 | `ZI-601` 개도 반복변동 | 계장 공기압 확인 정상, 포지셔너 점검 의뢰 | 변동 폭이 회차마다 커지는 중. 근무 중 3회 발생 |
| 4 | `TI-401` 인입온도 임계근접 | 외기 상승분으로 판단, 냉각수 온도 확인 정상 | 상한 여유 1℃. 오후 시간대 재확인 필요 |
| 5 | `FI-602` 공급유량 헌팅 | 유량 제어 밸브 헌팅 확인, 현장 Leak 없음, 제어 Gain 조정 검토 요청 | 10초 단위 급강하 5회. 컬럼 차압·온도도 동반 흔들림 |
| 7 | `ZI-801` 밸브 고착 | LV-801 수동 조작으로 동작 확인 불가, 포지셔너 점검 의뢰 | 개도 45% 고정 2시간. 탱크 레벨 추세로만 판별됨. 레벨 감시 강화 |
| 8 | `PI-203` 서지 전조 | 압축기 부하 낮춤, 안티서지 밸브 동작 확인 | 토출압 상승·유량 감소 동시 발생. 서지 직전 패턴. 재발 시 즉시 부하 감발 |
| 9 | `TI-403` Cold end 저온 이탈 | 터빈 팽창량 확인, 콜드박스 열밸런스 점검 | 시간당 1.5℃ 상승 3시간. 순도 저하로 이어질 수 있음 |
| 11 | `ZI-601`↔`FI-602` 상관 붕괴 | 개도 대비 유량 불일치 확인, 유량계 영점 점검 의뢰 | 개도는 정상 동작. 밸브 내부 또는 유량계 이상 의심 |
| 12 | `MI-804` 분석기 순간 이상값 | 샘플링 라인 압력 확인, 분석기 셀 점검 의뢰 | 999ppm 순간 표시 5회. 알람 미발생. 분석값 신뢰도 확인 필요 |
| 13 | `ZI-904` 공급압 반복 헌팅 | 공급압 제어 게인 낮춤, 벤트 개방량 확인 | ±5% 진동 5회. 누적 벤트 손실 확인 필요 |
| 14 | `SI-507` 회전수 순간 강하 | 베어링 온도·진동 확인 정상, 계측 이상 여부 확인 의뢰 | 20초간 800rpm 강하 후 복귀. 알람 미발생. 반복 여부 감시 |
| 15 | `AI-707`→`AI-803` 순도 동반 저하 | 컬럼 운전 조건 재조정, 탱크 제품 순도 별도 채취 의뢰 | 생산·탱크 순도 동반 하락. 분석기 이상 아님. 제품 격리 검토 필요 |

### 알람 발생 7종

| # | 대상 | 조치 문구 | 인계 문구 |
| --- | --- | --- | --- |
| 6 | `MI-302` 흡착탑 수분 파과 | A탑 재생 주기 단축 조치, 흡착제 교체 일정 검토 요청 | 후단 수분 3.4ppm 도달, 차압도 동반 상승. 콜드박스 결빙 주의 |
| 10 | `LI-806` LIN 탱크 고레벨 | 출하 스케줄 확인, 벤트 개방 대기 | 상한 초과. 출하 지연 시 벤트 손실 발생. 레벨 최우선 감시 |
| 16 | `TI-301` 흡착탑 과온 | 재생 히터 출력 차단, 히터 제어기 점검 의뢰 | 55℃ 도달. 흡착제 열화 우려. 재생 완료 후 성능 확인 필요 |
| 17 | `PI-901` GN2 공급압 저하 | LIN 액 밸브 조정 및 공급 밸브 개도 증대, 벤트 축소 | 4.35bar까지 하락. B/U 추가 가동 |
| 18 | `LI-701` HP Column 고레벨 | 이송밸브 개도 증대로 레벨 저하 유도 | 44% 도달. 캐리오버 시 LP 순도 저하. 레벨 감시 지속 |
| 19 | `PDI-103` 흡입 필터 차압 상승 | 필터 차압 상승 확인, 필터 교체 작업 의뢰 | 0.033bar 도달. 압축기 흡입 부하 증가. 교체 전까지 차압 감시 |
| 20 | `JI-206` 압축기 전력 급증 | 부하 확인 후 정상 복귀, 전기팀 통보 | 3,670kW 순간 도달 후 원복. 교대 시점 정상. 재발 여부 감시 |

### 문구를 쓸 때 지킨 것

현장 인계문이 문서와 다른 점을 살렸습니다.

- **직접 확인한 사실을 남깁니다** — `#5` 의 "현장 Leak 없음" 처럼, 아니었던 것도 적어야 다음 근무자가 같은 걸 다시 안 봅니다
- **누구에게 넘겼는지 적습니다** — 의뢰 / 요청 / 통보
- **조치가 없으면 없다고 적습니다** — `#2` 는 "별도 조치 없이 추이 감시"입니다. 빈칸이 아니라 판단의 기록입니다
- **현장에서 쓰는 말을 그대로 씁니다** — 리플럭스 · Leak · Gain · B/U · 감발

> `#17` 은 처음 초안에서 "수요처 통보"를 첫 조치로 적었는데 현장 검토에서 빠졌습니다. **공급압이 떨어지면 LIN 생산을 줄여 GN2 공급 여유를 먼저 만들고, 부족하면 B/U 를 추가 가동합니다.** 대외 통보보다 설비 조작이 앞섭니다.

---

## 시나리오 주입 데이터 생성

[`asu_dcs_overview.html`](asu_dcs_overview.html)의 **SCENARIO INJECT** 탭에서 만듭니다.

**절차** — 날짜·근무(주간/야간) 선택 → 방식 선택(랜덤 6~10종 / 직접 선택) → `시나리오 주입 & 생성`. 누를 때마다 **새로운 랜덤 데이터**가 생성되고, 직전 근무의 바로 다음 근무를 만들면 **근무를 넘는 이상이 이어집니다**.

주간(06~18) → 야간(18~06) → 다음날 주간 … 으로 이어지며, `app/config.py` 의 교대 구간(`day:6, night:18`)과 정확히 일치합니다.

**생성 방식**

1. 태그 53점의 12시간 정상 베이스라인을 만듭니다 (기저값 + 드리프트 + 노이즈)
   - `TI-101` 대기온도·`MI-102` 대기습도는 **24시간 일주기**를 따르고, 하류 태그가 외기에 끌려 움직입니다. 상세는 [`README.md`](README.md) 외기 연동 참고
   - 베이스라인은 **자동 발급된 난수 기준값 하나로 만든 연속 신호를 근무별로 잘라 쓰는** 방식입니다. 근무마다 기준값을 바꾸면 경계에서 값이 튀기 때문입니다
2. 시나리오를 랜덤 시간대에 주입합니다 — **총 6~10건** (이어지는 것 포함)
3. 태그 간 연동을 적용해 **주입한 변화가 다른 태그로 전파**되게 합니다
4. 외기 영향을 하류 태그에 전파합니다
5. 값 고정·스파이크처럼 전파되면 안 되는 것은 그 뒤에 덮어씁니다
6. 물리 한계로 클램프합니다 (`%`는 0~100, 나머지는 0 이상)

**동시 발생을 허용합니다.** 현장에서는 이상이 동시에 일어나므로, 같은 태그에서 두 시나리오가 겹칠 수 있게 두었습니다. 엔진이 겹친 상황도 잡아내는지 보는 것이 목적입니다. 답안지에 **어느 조합이 어느 태그에서 몇 분간 겹쳤는지** 기록합니다.

**단 두 조합만 겹침을 금지합니다.**

| 금지 조합 | 이유 |
| --- | --- |
| `#2` × `#8` (`TI-205`) | `#2`의 핵심이 "한 근무만 보면 알람이 안 울린다"인데, `#8`이 겹치면 H를 넘어 설계가 무너집니다 |
| `#1` × `#15` (`AI-707`) | 순도 하강이 이중으로 걸려 L(99.0%)을 침범합니다 |

**같은 시나리오가 이어지는 동안에는 다시 뽑지 않습니다.** 같은 이상이 겹쳐 쌓이면 파형이 두 배로 커져 답안지와 어긋납니다.

**근무 경계를 넘는 이상**

이상은 근무 경계에서 잘리지 않고 이어집니다. `#2` 토출온도 드리프트는 24시간짜리라 반드시 두 근무에 걸칩니다.

**앞 근무에서 시작된 드리프트가 다음 근무에서 알람에 도달하는 것** — 이것이 ENGRA가 해결하려는 문제 그 자체입니다. 앞 근무자가 인수인계에 적지 않으면 다음 근무자는 원인을 모릅니다.

답안지에 `carried_in`(앞 근무에서 이어짐)과 `continues_next`(다음 근무로 이어짐)를 표시합니다. 타임테이블에서는 `◀` `▶` 로 보입니다.

> `#2`가 H(125℃)에 실제로 닿는지는 **끝나는 시각의 외기**에 달렸습니다. 새벽에 끝나면 외기가 램프를 상쇄해 못 넘고, 오후에 끝나면 넘습니다. 그래서 답안지의 알람 여부는 선언값이 아니라 **생성된 데이터에서 실측**해 기록합니다.

**내보내기** — `app/README.md` 연결 규약을 따릅니다.

| 파일 | 내용 |
| --- | --- |
| `asu_<날짜>_<주간\|야간>_12h.csv` | 근무 1개 · `timestamp,tag,value` · 2초 주기 · 약 114만 행 |
| `asu_answer_<근무ID>.json` | 그 근무의 **답안지** — 시나리오·태그·시각·지속·알람 실측·겹침·근무 넘김 |
| `asu_answer_all.json` | **통합 답안지** — 전체 근무 합산 + 24종 커버리지 |

정답지를 **별도 파일로 분리**한 이유는, 데이터 파일에 정답이 섞이면 검출 엔진이 그걸 보고 맞힐 수 있어 성능 측정이 무의미해지기 때문입니다.

**포함률 측정** — 한 근무에는 6~10종만 들어가므로 `x/24`를 재려면 여러 근무를 합산해야 합니다. 통합 답안지에 `scenario_coverage`(커버된 시나리오 / 빠진 시나리오 / 비율)가 들어 있습니다. 실측으로 통상 **3~4근무면 대부분 나옵니다.**

**과거 기록** — 화면 하단에 근무별로 한 줄씩 남습니다 (날짜 · 근무 · 발생 이벤트 번호 · 동시발생 수 · 답안지 팝업). 브라우저에 보관되므로 창을 닫아도 유지됩니다.

**CSV 는 최근 3근무만** 다시 받을 수 있습니다. 근무 하나의 데이터가 메모리로 약 9MB라, 전부 들고 있으면 화면이 느려집니다. **생성 직후 CSV 와 답안지를 함께 저장하세요** — 그 이전 근무를 다시 만들면 새 랜덤 데이터가 되고, 예전 답안지와는 맞지 않습니다.

**재현성** — 화면에는 난수값을 입력하는 칸이 없습니다. 생성할 때마다 자동으로 새 값을 발급하므로 **누를 때마다 다른 데이터**가 나옵니다. 발급된 값은 추적용으로 답안지 `base_seed` 에 기록되지만 화면에서 되돌려 넣는 방법은 없으니, 같은 데이터가 다시 필요하면 CSV 를 보관하세요.

> 파일 저장은 GitHub Pages 주소에서만 됩니다. 아티팩트 미리보기 화면은 브라우저가 다운로드를 차단합니다.

---

## 다음 작업

- [x] 단순 알람 시나리오 5종 추가 → 총 20종 (Issue #1)
- [x] 태그 53점을 연결 규약에 반영 — `app/README.md` 갱신 완료
- [x] 시나리오별 **조치 문구** — 모의 과거 일지 시드로 확정, 20종 작성 완료 (Issue #3)
- [ ] 생성한 CSV를 정기영님 검출 엔진에 넣어 정답지와 대조 (포함률 측정)
- [ ] 순간 스파이크 전처리가 기준선을 오염시키지 않는지 통합테스트에서 확인 (Issue #2)