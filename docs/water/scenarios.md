# 수처리 특이 시나리오 정의

ENGRA가 수처리 Plant 데이터에서 감지해야 할 특이사항을 정의한다.
ASU `scenarios.md`와 같은 서식을 쓰고, 같은 방식으로 모의 데이터 생성기의 입력이자
감지 성능(포함률) 측정의 기준이 된다.

- 태그 정의: [`tag_master.csv`](tag_master.csv) (계측 태그 65점)
- 설비 정의: [`PLANT_SPEC.md`](PLANT_SPEC.md)
- **ASU 자산과 분리** — 이 문서와 태그 마스터는 `docs/water/` 아래에만 있다.
  엔진 정본인 `docs/tag_master.csv`와 `docs/scenarios.md`는 건드리지 않는다
- 1~4번은 CLAUDE.md「이상 시나리오(데모용 우선순위)」를 서식에 맞춰 옮긴 것
- 5~10번은 냉각탑 계통·TMS 계통 추가분

---

## 시나리오 목록

| # | 유형 | 대상 | 형태 | 크기 | 지속 | 중요도 | 판단 근거 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 드리프트 | `VPT-01` 흡인압 / TMP | 수 시간에 걸친 완만한 상승 | 104 → 122 kPa (시간당 +3.0) | 6시간 | 상 | **H(125) 미달이라 알람이 안 울린다.** 알람 전 드리프트 구간이 engra의 핵심 소구점 |
| 2 | 급등 | `TUR-01` 투과수 탁도 | 급상승 후 고탁도 유지 | 0.045 → 0.28 NTU (H 0.1) | 50분 | 상 | **알람 발생.** 막 파손 의심 — 막 교체 판단에 직결 |
| 3 | 연쇄 드리프트 | `TUR-02` 원수 탁도 | 상승 후 응집·막여과로 순차 전파 | 3.5 → 11 NTU (H 12 미달) | 3시간 | 상 | 3계열 공통 원인. 후단 `VPT-01`에 30분 지연으로 도달 |
| 4 | 계단 변화 | `FI-202A/B` Blower 공기량 | A/B 절체 순간 일시 변동 후 복귀 | −15% (833 → 708 Nm3/h) | 4분 | 중 | 짧아서 알람 미달. 스크러빙 부족이 반복되면 막 오염 가속 |
| 5 | 연쇄 | `FI-302` 냉각탑 공급 유량 | 냉각 부하 증가로 완만히 상승 | 55 → 84 m3/h (H 90 미달) | 4시간 | 상 | **단일 태그로는 안 잡힌다.** 여과수 탱크 수위 하강 → UF 증산 요구 → TMP 상승으로 이어짐 |
| 6 | 고착 + 드리프트 | `FI-602` 냉각탑 B/D 유량 | 유량이 한 값에 고정, 전도도만 상승 | 8.0 m3/h 고정 · `AT-04` 1200 → 2100 uS/cm | 2시간 | 중 | 유량값 자체는 정상범위라 알람 없음. **농축배수가 관리한계를 향해 가는 것은 전도도로만 보인다** |
| 7 | 동반 초과 | `TUR-03` Clarifier 상등수 탁도 → `QI-802` 방류수 SS | 침전 불량이 방류 수질로 전파 | 4 → 16 NTU · SS 6 → 22 mg/L (H 20) | 2시간 | 상 | **알람 발생. 법정 배출기준 초과 건이라 행정처분 대상.** 인수인계 누락 시 책임 소재가 불분명해진다 |
| 8 | 순간 이상값 | `QI-801` 방류수 TOC | 말도 안 되는 값으로 순간 폭등 후 3초 내 원복 | 8 → 350 mg/L (정상값의 약 44배) | 3초 × 근무 중 4회 | 중 | 알람 on-delay보다 짧아 미발생. **TMS는 전송값이라 순간값도 관제센터 기록에 남는다** — 분석기 셀 고장 전조 |
| 9 | 임계 근접 → 초과 | `LT-07` 냉각탑 B/D 집수조 수위 | 완만히 상승해 상한 돌파 | 48 → 87% (H 85) | 90분 | 중 | **알람 발생.** 방치 시 `LT-06`으로 넘어가 폐수 회수 계통 전체가 밀린다 |
| **10** | **정상 작업 — CIP 세정(MC)** | `VPT-01` TMP (`TT-01`·`JI-401`·`AT-02` 연쇄) | 약식 세정으로 TMP 회복 | TMP 122 → 105 kPa · 세정온도 상온 → 98℃ | 90분 | 중 | **이상이 아니라 계획된 작업.** 다음 근무자는 TMP가 왜 떨어졌는지 알아야 한다 (ASU `#21`·`#22` 출하와 같은 구조) |

---

## 전파 경로와 기대 탐지 결과

위 표가 "무슨 일이 일어나는가"라면, 이 표는 **"그래서 어느 태그에서 몇 분 안에 잡혀야 하는가"**다.
통합테스트 판정 기준으로 그대로 쓴다.

| # | 트리거 태그 | 주입 파형 | 전파 (지연) | 기대 탐지 태그 | 탐지 목표 | DCS 알람 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `VPT-01` | 램프 (+3.0 kPa/h · 6시간) | `JI-201A` (즉시), `FI-201` (60초) | `VPT-01` | 90분 이내 | 미발생 (H 125 대비 3 kPa 여유) |
| 2 | `TUR-01` | 계단 상승 후 유지 | `AT-01` (60초) | `TUR-01` → `AT-01` | 10분 이내 | **발생** (H 0.1 NTU 초과) |
| 3 | `TUR-02` | 램프 (+2.5 NTU/h · 3시간) | `CI-904` (60초), `VPT-01` (1800초) | `TUR-02` → `VPT-01` | **알람보다 먼저** (60분 이내) | 미발생 |
| 4 | `FI-202A` | 딥 (−15%, 4분) × 1 | `PI-202A` (즉시), `VPT-01` (600초) | `FI-202A` | 발생 즉시 | 미발생 |
| 5 | `FI-302` | 램프 (+7.3 m3/h/h · 4시간) | `LT-03A` (120초), `AT-04` (300초) | `FI-302` × `LT-03A` 조합 | 60분 이내 | 미발생 |
| 6 | `FI-602` | 값 고정 (stuck, 2시간) | `AT-04` 추세 이탈 (600초) | `AT-04` | 60분 이내 | 미발생 |
| 7 | `TUR-03` | 지수 상승 | `QI-802` (240초) | `TUR-03` → `QI-802` | **알람보다 먼저** (40분 이내) | **발생** (`TUR-03` H, `QI-802` H) |
| 8 | `QI-801` | 스파이크 (×44, 3초) 반복 | — | `QI-801` | 3회 발생 내 | 미발생 |
| 9 | `LT-07` | 램프 (+26%/h · 90분) | `LT-06` (300초) | `LT-07` | **알람보다 먼저** (45분 이내) | **발생** (H 85% 초과) |
| 10 | `VPT-01` | 하강 램프 + `TT-01`·`JI-401` 기동 | `AT-02` (즉시), `CI-902` (즉시) | `VPT-01` 하강 + `TT-01` 기동 | 30분 이내 | 미발생 (계획 작업) |

---

## 제약과 주의

**① 생성기는 링크를 1단계만 전파한다.**
ASU `scenarios.md`에 기록된 `applyLinks` 동작이다 — `add[]`를 원본에서 계산해 한 번에 더하므로
2단계는 닿지 않는다. `#5`가 여기 걸린다.

```
FI-302 ──links──▶ LT-03A, AT-04        ← 여기까지 간다
LT-03A ──links──▶ FI-301A              ← 여기는 안 간다
```

물리적으로는 냉각탑 수요 증가가 탱크 수위를 낮추고 그것이 UF 증산으로 이어지는 게 맞지만,
**정답지는 데이터에 맞춘다.** `#5`의 영향 태그는 `FI-302`·`LT-03A`·`AT-04` 3개까지만 적는다.
ASU에서 `#3`·`#11`이 선언 7개 / 실제 3개였던 것과 같은 문제이고, 같은 방식으로 처리한다.

**② 드리프트 진폭이 알람선을 침범하면 안 된다.**
ASU `LI-806` 결함(근무당 오탐 1.00건)의 원인이다. 진폭은 정상범위 폭 기준으로
**1단 4.2% · 2단 8.4% · 3단 16.8%**(주 드리프트 + 보조 0.4배)이고, `nominal ± 진폭`이
`H`/`L`을 넘으면 시나리오를 안 넣어도 상시 알람이 뜬다.
→ 현재 `tag_master.csv` 65태그 전수 검사 **침범 0건**. 태그를 추가하거나 한계값을 바꾸면 다시 검사할 것.

**③ 계열 구분은 기기 태그로만 된다.**
계측 태그를 CLAUDE.md 표기대로 두기로 해서 `VPT-01`·`TUR-01`·`LT-02`는 3계열 대표값 하나다.
`#2`(막 파손)는 어느 계열인지 계측값으로 못 가린다. 계열을 보여야 하면
`JI-201A/B/C`·`ZI-201A/B/C`·`FI-203A/B/C`를 써야 한다.

**④ TMS 계통은 실설비 확인 전이다.**
`PH-02`·`QI-801`~`QI-804`·`FI-603` 6점은 CLAUDE.md에 근거가 없다. TMS는 공공수역 **방류** 지점에
붙는 법정 설비인데 CLAUDE.md는 "상등수 재이용"이라고만 돼 있어 방류 경로 자체가 정의돼 있지 않다.
`#7`·`#8`은 이 6점 위에 서 있으므로, 실설비에 TMS가 없으면 두 시나리오와 6개 태그를 같이 뺀다.

**⑤ 간헐 운전 태그의 정상 구간 정의가 남아 있다.**
`TT-01`·`JI-401`·`CI-906`은 CIP 세정 중에만 유효하다. `#10`이 이 태그들을 처음 쓰는 시나리오라,
세정 미실시 구간을 무엇으로 채울지(정상범위 유지 / 운전모드 플래그 분리) 생성기 설계 시 결정해야 한다.
