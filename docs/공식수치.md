# 공식 수치 — 제출·발표에 쓰는 숫자의 단일 출처

문서마다 숫자가 흩어져 옛 값이 남는 사고가 있었다(기획서 PDF 의 「오탐 1.5/2.2」).
**제출물·발표에 숫자를 쓸 때는 이 문서만 본다.** 여기 없는 숫자는 쓰지 않는다.

> 마지막 갱신 **2026-09-14** (알람 선행 검출 ①② 현재 코드 재실행 값으로 교정 · ⑧ AI 소요 시간 신설) · 검출 수치 측정자 임도영(QA) · 모든 값은 `LI-806` 수정 후 재생성 데이터 기준
> 직전 갱신 2026-08-29 (CUSUM 구간 정밀화 반영)

## 재현 방법

```bash
export ENGRA_LLM=off PYTHONIOENCODING=utf-8 ENGRA_DB=/tmp/score.db
python3 app/cli.py init
for f in data/asu_shift_*_12h.csv; do python3 app/cli.py ingest "$f"; done
python3 app/cli.py run 2026-08-21-day && python3 app/cli.py run 2026-08-21-night
python3 tools/score.py data/asu_answer_key.json
```

알람 선행 검출(①②의 마지막 줄)도 같은 `tools/score.py` 출력의 「알람 선행 검출」 줄이다. 별도 명령이 없다.

---

## ① 검출 성능 — 제출용 `data/` 2근무

| 항목 | 값 |
| --- | --- |
| **주입 건 포함률** | **22/22 (100%)** — 미탐지 0건 |
| 시나리오 종 커버 | 20/20 종 |
| 시각 일치도 IoU | 전체 **0.73** (중앙 0.80 · ≥0.5 18/22) |
| 〃 국소형 16건 | 0.75 (중앙 0.81 · ≥0.5 13/16) |
| 〃 추세형 6건 | 0.70 (중앙 0.76 · ≥0.5 5/6) |
| 오탐 | 2건 / 이벤트 79건 = **근무당 1.0건** |
| 〃 복귀 구간 제외 | **0건** (2건 전부 주입 종료 직후 복귀) |
| 알람 선행 검출 | 7/10 건 · 최대 **+616분** |

> 알람 선행 검출은 2026-09-13 현재 코드(main) 재실행 값 · 옛 값 7/11·+706 은 트리 8abb5d3 시점.

## ② 검출 성능 — `data/sim` 6근무 (분모가 큰 쪽)

| 항목 | 값 |
| --- | --- |
| **주입 건 포함률** | **46/46 (100%)** — 미탐지 0건 |
| 시나리오 종 커버 | 21/21 종 |
| 시각 일치도 IoU | 전체 **0.69** (중앙 0.76 · ≥0.5 35/46) |
| 〃 국소형 34건 | 0.68 (중앙 0.77 · ≥0.5 24/34) |
| 〃 추세형 12건 | 0.72 (중앙 0.73 · ≥0.5 11/12) |
| 오탐 | 11건 / 이벤트 175건 = **근무당 1.8건** |
| 〃 복귀 구간 제외 | **0건** (11건 전부 복귀) |
| 알람 선행 검출 | 16/18 건 · 최대 **+560분** |

> 알람 선행 검출은 2026-09-13 현재 코드(main) 재실행 값 · 옛 값 16/18·+666 은 트리 8abb5d3 시점.

> **오탐은 두 줄로 낸다.** 전체 수를 감추지 않고, 그중 복귀가 몇 건인지를 함께 낸다.
> 복귀는 「주입이 끝나고 값이 돌아오는 구간」 으로, 정답지가 주입 구간만 적어서 생기는 차이다.

## ③ 정상(결함 없음) 데이터 오탐

| 항목 | 값 |
| --- | --- |
| 대상 | 기준선 **14근무** · 시나리오 0건 · 태그 53점 × 12시간 |
| 검출 이벤트 | **0건** |
| 초안 항목 | **0개** |

## ④ 장기 사용 — 50일(100근무) 연속

| 항목 | 값 |
| --- | --- |
| DB 크기 | 7근무째부터 **평평** — 605.8MB → 611.3MB (93근무 동안 +5.5MB) |
| 원본 행수 | 7근무째부터 **6,868,853행 고정** (`rotate_raw` 3일 보관) |
| 검출 시간 | 평균 **18.3초** (1~10근무 20.5초 · 90~100근무 18.3초 — 누적 저하 없음) |
| 누적 | 이벤트 2,400건 · 초안 575항목 · 확정 일지 100건 |

## ⑤ AI 가 하는 일 — 7근무 실측

| 항목 | 값 |
| --- | --- |
| 과거 조치 판정 | 후보 **50건 → 채택 33 · 기각 17** |
| 판정 사유 기재 | **31/31 항목 (100%)** |
| 중요도 재판정 | 52항목 중 **10건 (19%)** |
| 없는 조치를 지어냄 | **0건** — 과거 일지에 조치가 없으면 비워 둠(4건) |
| 숫자 환각 | 본문 숫자 **423개 중 0개** |
| 재현성 | 같은 근무 2회 — 중요도 **9/9** · 인계대상 **9/9** 일치 |

## ⑥ 데이터 건전성

| 항목 | 값 |
| --- | --- |
| HH/LL 침범 | 0건 |
| 금지조합 위반 | 0건 |
| 적재 구간 충족률 | 100.0% |
| 순도 태그 100% 초과 | 0건 |
| 정답지 영향 태그 | 24종 전수 대조 — 파형과 일치 (`#3`·`#11` 정정 완료) |

---

## ⑦ 시각 정밀도 — 외부 검토 지적으로 추가 (2026-08-29)

「포함률 100%」 만 내면 **검출 구간이 얼마나 넓은지**가 감춰진다. 시간축(초 단위)으로도 낸다.

| 항목 | `data/` 2근무 | `data/sim` 6근무 |
| --- | --- | --- |
| 검출 지연 중앙값 | **−2분** | **−0분** |
| 주입 뒤에 잡은 것 | 6/22건 (최대 +49분) | 15/46건 (최대 +115분) |
| 시간축 재현율 | 0.86 | 0.87 |
| **시간축 정밀도** | **0.46** | **0.49** |
| 시간축 F1 | 0.60 | 0.63 |

- **지연이 음수**인 것은 추세형 창이 주입보다 먼저 열리기 때문이다. 대부분 주입 시작
  시점이나 그 이전에 이미 창이 열려 있다
- **정밀도 0.46 은 검출 구간이 정답의 2.2배를 덮는다**는 뜻이다. 넓게 잡는 성질을 정면으로
  드러내는 숫자라 **감추지 않고 같이 낸다**
- 2026-08-29 에 드리프트 보고 구간을 CUSUM 으로 좁혀 정밀도 0.40 → 0.46, 검출 지연
  중앙 −34분 → −0분 이 됐다. 검출 여부는 안 바꾸고 보고 구간만 좁힌 것이다

## ⑧ AI 소요 시간 — 한 근무 초안의 AI 서술·판정 (2026-09-14 신설)

AI 소요 시간이 자료마다 다르게 적혀 있어 정본을 알 수 없다는 심사 지적(03)으로 신설했다. 흩어져 있던
값을 **이력으로** 모은다. 항목 수와 측정 회차가 다르면 시간이 다르니, 쓸 때는 **항목 수와 조건을 붙인다.**
적재·검출 시간은 넣지 않는다 — AI 서술·판정 단계만의 시간이다.

| 측정일 | 조건 | 항목 수 | 시간 | 출처 |
| --- | --- | --- | --- | --- |
| 2026-08-26 | 순차 `BATCH=5` · cli · 정답지 주간 근무 | 16 | **5분 42초** (1회) | 커밋 8dc3d22 본문 · `HANDOFF.md` 4-1절 ⑦ |
| 2026-08-26 | 〃 · 정답지 야간 근무 | 15 | **6분 30초** (1회) | 커밋 d34960f 본문. `HANDOFF.md` 4-1절의 「15~16항목 5.5~6.5분」 은 이 두 회를 뭉뚱그린 값 |
| 기재 2026-08-31 (측정 근무 미기재) | 〃 | 13~16 | **387~435초 = 6.5~7.2분** (5항목 묶음당 2~3분) | `docs/과제기획서_rev2.html` 3-5·5-1 (커밋 afbc876 에 처음 나온다) |
| 2026-08-28 야간 · 08-29 주간 근무 (09-01 확인) | 〃 · 라이브 서버 journalctl | 8 · 6 | **3분 23초 · 3분 10초** — 「6~8항목 3분대」 | 커밋 4028324 본문 |
| (추정 · 08-28) | 〃 · 플랜트 정지 근무 | 약 10 (이벤트 39건 압축) | **약 20분** — 실측 아님 | `docs/scenarios.md` 「플랜트 정지」 표 |
| ~~08-27 ~ 09-01 표기~~ | 측정이 아니라 적어 둔 값 | — | ~~7~10분~~ **폐기** — 근거 없는 상한 | 커밋 aeb1ae4(08-27)에 처음 · 09-01 정정(커밋 4028324 · 9215dbb) |
| **병렬화 후 (2026-09-14)** | | | **실측 대기** | |

- 병렬화 전 값을 한 줄로 쓸 때는 본선 제출 문서와 같은 **「순차 13~16항목 6.5~7.2분 · 6~8항목 3분대」** 를 쓴다.
  같은 조건의 08-26 실측 중 6분 30초는 이 범위 안이고 5분 42초는 범위보다 빠르다 — 회차에 따라 이만큼 흔들린다는 기록이라 이력으로 남긴다.
- 병렬화 실측(같은 근무로 순차 대 병렬 전후 비교 — `결선_실행계획.md` 3-1)이 들어오면 마지막 행을 채운다. 그 뒤로는 **그 행이 현재값**이고 위 행들은 이력이다.

## 쓸 때 반드시 함께 밝힐 것

1. **가상 DCS 데이터다.** 실제 공정 데이터가 아니다. 먼저 밝히면 강점이 되고,
   심사위원이 먼저 찾아내면 약점이 된다
2. **정답지도 우리가 만들었다.** 다만 채점이 헐겁지 않은지는 실측했다 —
   정답지를 60분 옮기면 포함률이 **100% → 75%** 로 무너진다
3. **실제 데이터에서는 낮아질 수 있다.** 그 검증이 Phase 1 이다
4. **IoU 는 세 줄을 다 낸다.** 전체를 빼고 국소형만 쓰면 숫자를 고른 것이 된다

## 쓰면 안 되는 숫자

| 옛 값 | 어디에 남아 있나 | 왜 안 되나 |
| --- | --- | --- |
| ~~`97.9% 검출`~~ | ~~`docs/intro.html`~~ | **출처 없음이라 교체함** (2026-08-29) → 「가상 DCS 6근무 · 심어둔 이상 46건 · 미탐지 0건」 |
| `오탐 1.5/2.2` | 기획서 PDF | 08-28 값. 지금은 1.0/1.8(복귀 제외 0/0) |
| `오탐 근무당 0.8건` | `제출_체크리스트.md` | 엔진 자체 픽스처(`selftest.py`) 값 — **독립 측정이 아니다** |
| `오탐 근무당 11건` | `HANDOFF.md` | `LI-806` 수정 전 값 |
| `포함률 47/48 (98%)` · `21/22 (95%)` | `HANDOFF.md`·`status.md`·기획서·검증결과정리 rev2 (08-26 첫 독립 측정 — 이력 문맥) | `#13` 수정 전 값. 지금은 22/22 · 46/46 (①②) |
| `알람 선행 7/11 · +706분` / `16/18 · +666분` (「최대 706분」 포함) | `docs/scenarios.md`·기획서·검증결과정리 rev2 (09-14 교정) · `제출/03`(본선 제출 보관본 — 수정 대상 아님) | 트리 8abb5d3 시점 값. 현재 코드는 **7/10 · +616 / 16/18 · +560** (①②) |
| `오탐 7건 · 근무당 1.2건` (6근무) | `docs/scenarios.md` 3차 시험 절 (이력 표시) | 08-29 CUSUM 정밀화 전 값. 지금은 11건 · 1.8건(복귀 제외 0) (②) |
| `IoU 0.71 / 0.68` (전체) | `docs/scenarios.md` 08-29 결정·실험 기록 (이력 표시) | 08-29 CUSUM 정밀화 반영(ad5eb63) 전 값. 지금은 0.73 / 0.69 (①②) |
| `AI 7~10분` | 구판 기획서·시연 대본 수정점(#14)·`HANDOFF.md` | **폐기** — 근거 없는 상한 (⑧) |
| `6.5~7.2분` · `3분대` · `5분 42초` · `5.5~6.5분` · `3~7분` · `약 20분` 을 조건 없이 한 값으로 | 기획서·검증결과정리·설명서 rev2 · `docs/scenarios.md` · `HANDOFF.md` · `docs/결선_예상질문.md` | **⑧로 통일됨** — 항목 수·조건이 다른 값이다. ⑧ 의 표기(항목 수·조건 포함)로만 쓴다 |
| `시연 영상 64초 · 무편집·실시간` | `제출_체크리스트.md`·`HANDOFF.md`·`status.md`·`docs/문서안내_rev2.html` (09-14 교정) | 09-01 교체본은 **4분 34초 · 0:00~6:52 구간 1.5배속(화면 표시)** — 정본은 `제출_체크리스트.md` 「제출 직전 공통 점검」 |
