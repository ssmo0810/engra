# 현재 상태

## 일정 (Notion 허브 확인, 2026-08-24)

| 시점 | 내용 |
| --- | --- |
| **09-03 (목) 23:59** | **본선 제출 마감** — 마감 시점 폴더 상태를 그대로 평가. 이후 수정 반영 안 됨 |
| 09-09 (수) 17:00 | 결선 진출 10팀 발표 |
| 09-14 (월) ~ 09-15 (화) | 결선 해커톤 — **팀 전원 오프라인 참석 필수** (불참 시 차순위 팀 진출) |
| 09-15 (화) | 통합 시상식 |

제출처는 운영측이 배정하는 **Google Drive 팀 전용 폴더**. 중간 제출 없음.

## 평가 배점 (100점)

| 기준 | 배점 | 측정지표 | 제출 폴더 |
| --- | --- | --- | --- |
| 구현 완성도 | 35 | 기능적 완성도 25 · 재현 가능성 10 | 02 · 03 |
| SUPEX 추구 | 30 | 문제정의 10 · 목표수준 10 · 임팩트범위 10 | 01 |
| 혁신성 | 20 | 기존방식 차별성 10 · AI 네이티브 설계 10 | 01 |
| 집요함과 패기 | 15 | 문제해결 과정의 집요함 15 | 04 |

폴더별 배점: **01 과제기획서 50점** · 02 결과물 25점 · 03 검증환경 10점 · **04 제작과정 15점**

> 심사위원은 제출된 자료만으로 판단한다. **문서에 없는 내용은 반영되지 않는다.**
> 작동하는 앱 URL 이 열리지 않으면 구현 완성도를 확인할 수 없다.

## 제출 폴더 구조 (이름 변경·삭제·추가 금지)

평가가 이 구조를 그대로 읽어가므로, 이름이 다르면 그 항목은 **비어 있는 것으로 처리**된다.

```
001. 팀명/
├── README_00. 먼저 읽어주세요
├── 01. 과제 기획서/        갱신한 기획서
├── 02. 결과물/
│    ├ 1. 시연 영상/         5분 이내, 끊김 없이 시작~결과
│    ├ 2. 결과물 원본/       접속 주소 · 외부 개방 여부 · 저장소 주소와 공개 설정
│    └ 3. 설명서·매뉴얼/
├── 03. 검증 환경/          검증 결과 정리 · 데이터 세트 · 동작 확인 자료
└── 04. 제작 과정/          추진 계획서 · 개발 일지 · 회의록 · 난관과 극복
```

## 진행 상황

| 영역 | 담당 | 상태 |
| --- | --- | --- |
| 시나리오 20종 · 태그 53점 · 데이터 생성기 | 임도영 | ✅ |
| 검출 엔진 `engine/` | 정기영 | ⏳ 미착수 (#4) |
| 배선 `app/` — 적재→검출→초안→승인→확정 | 박경모 | ✅ 전 구간 동작 |
| 데모 DCS 화면 | 임도영 → 박경모 이식 | ✅ ASU 개관도 53점 |
| 초안 생성 LLM | 박경모 | ⏳ 지금은 문장 틀 |
| 시연 영상 | — | ❌ 미착수 |
| 01 기획서 갱신 (50점) | — | ❌ 예선본 그대로 |
| 04 제작 과정 (15점) | 박경모 | ✅ 커밋·이슈에서 자동 생성 (`tools/journal.py`) — 개발일지 47건·난관 12건·회의록 |

**임시 엔진(stub)** 이 돌고 있다. 알람 한계를 넘은 것만 잡으므로 시나리오 20종 중
**알람이 울리는 7종**(6·10·16~20)만 사정권이고, 알람 미발생 **13종**은 구조적으로 못 잡는다.
이 7/13 은 `docs/scenarios.md` 에서 도출한 설계값이고 **포함률 실측은 미실시**다 —
정답지(`asu_answer_key.json`)가 아직 없다(#5).

## 배포 구성 (규정 준수 상태)

| 항목 | 상태 | 근거 |
| --- | --- | --- |
| 저장소 | **비공개** (익명 접근 404) | FAQ Q9 "반드시 비공개로 설정" |
| 사이트 URL | **공개** (익명 접근 200) | 보안 가이드 — 배포용 앱에 가상 데이터만 |
| Pages 방식 | **브랜치 직접 배포** (Actions 미사용) | 아래 참고 |

**비공개 저장소에서 Actions 가 돌지 않는다.** 원인 확정 — **계정의 Actions 분이 다른
프로젝트에서 소진됐다**(팀장 확인). 공개 저장소는 분을 쓰지 않지만 비공개는 쓴다.
`echo` 한 줄만 하는 워크플로도 스텝 진입 없이 즉시 실패했다.
CI 는 팀장이 로컬에서 돌린다(`bash tools/smoke.sh`) — Actions 기반 CI 는 검토 대상이 아니다.

Actions 를 안 쓰는 브랜치 직접 배포로 전환해 해결했다. `main` 에 푸시하면 GitHub 자체
빌더가 서비스한다(`.nojekyll` 이 Jekyll 을 끈다). 워크플로 파일은 제거했다.

## 최근 완료

- 데모 DCS 를 임도영님 ASU 개관도로 교체 (태그 53점)
- 프로토타입 화면을 목업 디자인과 일치
- 프로토타입 골격 — 저장소 6종 · 엔진 포트 · 승인 · 확정 일지 순환
- 시나리오 20종 (#1)
