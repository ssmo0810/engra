# engra 저장소 작업 규칙

팀 앙그라쥬 3인이 각자 Claude Code로 이 저장소를 동시에 작업한다.
아래 규칙은 충돌과 덮어쓰기를 막기 위한 것으로, **예외 없이 지킨다.**

## 시작과 종료

- **작업 시작 전 반드시 `git pull --rebase`.** 다른 사람이 이미 푸시했을 수 있다.
- **한 가지 작업이 끝나면 바로 커밋하고 푸시한다.** 오래 들고 있을수록 충돌이 커진다.
- 푸시가 거절되면(non-fast-forward) `git pull --rebase` 후 다시 푸시한다. 절대 `--force` 를 쓰지 않는다.

## 담당 영역 — 남의 파일을 고치지 않는다

| 파일 | 담당 |
| --- | --- |
| `index.html` (소개 페이지) | 박경모 |
| `demo/index.html` (화면 데모) | 박경모 |
| `README.md`, `TEAM_GUIDE.md` | 박경모 |
| `.github/`, `.nojekyll`, `CLAUDE.md` | 박경모 |
| `app/` (수집부·저장소·승인·화면) | 박경모 |
| `engine/` (검출 알고리즘) | 정기영 |
| `docs/` (시나리오·검증 기준) | 임도영 |

프로토타입 개발이 시작되면 각 폴더 안에서 아래 산출물을 만든다.

| 경로 | 담당 | 내용 |
| --- | --- | --- |
| `app/` | 박경모 | 수집부, 저장소, 승인 처리, 화면, 스케줄러, 통합 |
| `engine/` | 정기영 | 요약·베이스라인·검출 알고리즘, 사례 검색, 초안 생성 |
| `docs/`, `data/`, `tests/` | 임도영 | 특이 시나리오, 모의 데이터, 테스트 |

폴더 사이에 주고받는 데이터 형식은 [`app/README.md`](app/README.md) 의 **연결 규약**을 따른다.
규약을 바꿔야 하면 **바꾸기 전에 팀에 알린다.**

## 프로토타입 실행

설치할 것이 없다. 파이썬 3.10 이상이면 된다.

```bash
python3 app/cli.py serve     # 웹 화면 (누구나)
bash tools/smoke.sh          # 배선 전 구간 확인 — app/ 을 고칠 때만 (박경모)
```

**검출 엔진은 `engine/api.py` 가 있으면 자동으로 그것을 쓴다.** 없으면 임시 엔진이 돈다.
실행 첫 줄과 화면 오른쪽 위에 `engine` / `stub` 으로 표시되니 어느 쪽이 도는지 바로 보인다.
규약과 시작하는 법은 [`app/README.md`](app/README.md) 의 "엔진 자리" 절에 있다.

`app/engra.db` 와 `app/sample_*.csv` 는 커밋하지 않는다(`app/.gitignore`).
저장소에 실제 운전 값이 섞여 들어가는 것을 구조적으로 막기 위한 것이다.

담당 밖 파일을 고쳐야 하면 **먼저 담당자에게 알리고** 작업한다.

## 서로에게 요청하고 알리기

팀원 간 요청·질문·공유는 **GitHub Issues** 로 한다. 파일에 적으면 충돌이 나고, 채팅에 적으면 사라진다.

- **작업 시작 전 자기 앞으로 온 이슈를 먼저 확인한다**: `gh issue list --assignee @me`
- 남에게 필요한 것이 생기면 이슈를 만들고 담당자를 지정한다: `gh issue create --title "[요청] ..." --assignee <아이디>`
- 처리했으면 닫는다: `gh issue close <번호> --comment "처리 내용"`
- 이슈 번호는 커밋 메시지에 적어 연결한다 (예: `... (#3)`).

담당 폴더: `app/` 박경모 · `engine/` 정기영 · `docs/` 임도영

## 커밋 — 커밋 메시지가 곧 제출물이다

본선 「04. 제작 과정」은 **15점**이고, 평가 기준에 이렇게 적혀 있다.

> 결과물만으로는 드러나지 않는 부분이라 별도로 기록해 주셔야 확인할 수 있습니다.

그래서 일지를 따로 쓰지 않는다. **커밋 메시지와 이슈를 모아 제출 문서를 자동 생성한다**
(`tools/journal.py`). 마감에 몰아서 되살릴 수 없는 자료라, 지금 남기는 것이 전부다.

- `git add` 는 **수정한 파일 경로를 명시**한다. `git add -A` / `git add .` 금지.
- 커밋 하나에 한 가지 변경만 담고, 메시지 첫 줄에 무엇을 바꿨는지 한국어로 적는다.
- **본문에 왜 바꿨는지와 무엇으로 확인했는지를 적는다.** 이 두 줄이 개발 일지가 된다.
- 문제를 만나 고쳤으면 **그 사실을 본문에 적는다** — 발견·결함·틀렸·재현 같은 서술이 있는
  커밋이 「난관과 극복 사례」로 자동 수집된다. 조용히 고치면 15점에서 사라진다.
- 다른 도구를 썼으면 `Co-Authored-By:` 로 남긴다. FAQ Q5 가 제작 과정에 도구를 밝히라고 한다.
- `main` 에 대한 force-push, 하드 리셋 금지.

회의를 했으면 **이슈로 남긴다.** 이슈가 곧 회의록이라 따로 정리할 것이 없다.

**문서 생성과 검증 도구는 박경모 담당이다.** 정기영·임도영 세션은 `tools/` 를 돌릴 필요가
없다 — 커밋 메시지에 왜·확인방법을 적고 이슈로 소통하면 그것으로 충분하다.

## 큰 HTML 파일을 고칠 때

`index.html`, `demo/index.html` 은 한 파일이 수만 자다.

- **파일 전체를 다시 쓰지 말고 필요한 부분만 수정한다.** 전체 재작성은 다른 사람의 변경을 통째로 덮어쓴다.
- 수정 후 브라우저로 직접 열어 동작을 확인한 뒤 푸시한다.

## 배포

- `main` 에 푸시하면 GitHub 이 자동으로 사이트를 다시 만든다 (30초~2분).
  브랜치 직접 배포이므로 Actions 를 쓰지 않는다 — 비공개 저장소에서 Actions 가 돌지 않아
  그렇게 바꿨다. 워크플로 파일을 되살리지 마라.
- 실수로 잘못 배포했으면 해당 커밋을 **Revert** 한다.

## 보안 — 저장소는 비공개, 사이트 주소는 공개다

해커톤 규정(FAQ Q9)이 저장소를 비공개로 두라고 요구한다. 공개로 되돌리지 마라.
반면 배포된 사이트 주소는 열려 있으므로, **화면에 올라가는 것은 누구나 본다.**

- 사내 기밀, 개인정보, 사내 시스템 원본 데이터·로그·화면 캡처를 올리지 않는다.
- 실제 설비 태그 체계와 실제 운전 수치를 쓰지 않는다. **현재 데모의 모든 값은 가상 데이터다.**
- API 키·비밀번호·접속 토큰·사내 시스템 주소를 어디에도 남기지 않는다.
- 판단이 애매하면 올리기 전에 팀장에게 확인한다.
