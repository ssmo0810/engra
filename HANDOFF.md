# 인계 — 다음 세션은 이 문서부터 읽는다

SK AI 해커톤 2026 · AI Solution 리그 본선. 팀 **앙그라쥬**, 서비스 **ENGRA**.
저장소 `ssmo0810/engra` (**비공개**). 작성 2026-08-25.

읽는 순서: **이 문서 → [status.md](status.md) → [CLAUDE.md](CLAUDE.md)**.
그 밖의 문서는 지금 하는 일이 명시적으로 가리킬 때만 읽는다.

## 대회 공식 정보 — 원문 주소

**https://likelion.notion.site/2026-AI-Solution-3b344860a4f480248a67e7218a6a57b6**

공개 페이지지만 **SPA 라 WebFetch 로는 못 읽는다.** 브라우저(playwright 등)로 열어
`document.body.innerText` 를 읽어야 내용이 나온다. 표 안의 문서는 행을 클릭해
`열기`(영문 로케일이면 `OPEN`) 버튼을 눌러야 열린다.

**2026-08-25 에 원문을 다시 읽고 대조했다.** 읽은 것 — `프로젝트 제출 방법` · `본선 평가 기준` ·
`보안·데이터 가이드` · `FAQ` · `Ground Rule` · 허브 첫 화면. 대조 결과 **status.md 의 배점표와
일정은 원문과 100% 일치**한다(마감 09-03 23:59 포함). FAQ Q5(도구 명시)·Q9(저장소 비공개)
인용도 정확하다.
아직 안 읽은 것: `결선 제출 방법`, `결선 평가 기준`, `시상`(Ground Rule 로 대체 확인), `온라인교육 안내`, `위치/교통`.

**원문에서 새로 확인해 반영한 것 (2026-08-25)**
- **양식 자유 · 분량과 디자인 완성도는 평가 대상 아님.** 파일명은 `과제기획서_팀명.pdf` 권장
  → `제출/과제기획서_앙그라쥬.pdf` (15쪽) 생성해 둠.
- **팀원의 이름·소속·역할은 개인정보가 아니며 제출물에 명확히 밝혀야 한다**(보안 가이드).
  가려야 하는 것은 제3자 정보이고, 저장소 비공개의 관심사는 **사내 시스템명·접속 정보**다.
- **「03. 검증 환경」에 모사 데이터를 쓴 사실과 그 방식을 적으면 원본과 동일하게 평가**한다(보안 가이드). 반드시 적을 것.
- 평가가 보는 「실제 업무 데이터」 = 원본이 아니라 **유사한 구조·같은 난이도**. 태그 53점·12시간·114만 점 실측이 여기에 대응한다.
- **Value Proposition** 이 Ground Rule 의 본선 제출물 목록에 있다(과제 기획서·프로토타입·설명서/매뉴얼/Value Proposition). 02 폴더에 넣는다.
- **오픈소스 사용 시 라이선스 준수·출처 명시 의무**(Ground Rule). 우리는 외부 의존 0(실측)이라 해당 없음 — 기획서 4-3 에 명시했다.
- **LLM API 비용 지원 팀당 최대 5만원**(네이버 상품권, 사용량·금액 실비 인증). Claude 연결(항목 4) 예산.
- 세미나·OT 참석 여부는 **평가에 반영되지 않는다**. 8/25(화) 19:00 OT, 8/27(목) 세미나 주제 = AI로 가상 데이터 만들기(#7 클램프 건과 관련).
- 시상: 1등 1팀 1,500만원(+Claude Max 1년), 2등 3팀 900만원, 3등 6팀 600만원, 참가상 3만원 —
  단 **제출 형식에 맞게 성실히 작성한 팀에 한함**. 결선 10팀은 전원 수상.
- 본선 심사평은 9/14 결선 당일 공개. 마감 후 수정·소통 불가.

---

## 1. 지금 무엇을 만들고 있나

12시간 근무 구간의 운전 데이터를 읽어 **인수인계 초안**을 만들고, 근무자가
채택·제외하고 코멘트를 달아 확정하면, 그 확정 일지가 **다음 근무 초안의 과거 조치로
되돌아오는** 순환. 이 순환은 이미 코드로 돈다.

화면 흐름은 **DCS(정본) → RTDB(구간 추출) → ENGRA(초안 검토) → 일지 조회**.
DCS 가 태그 체계의 기준이고 뒤 화면들이 그것을 인용한다.

---

## 2. 마감과 배점 (Notion 허브 실측, 절대 틀리면 안 되는 값)

| 시점 | 내용 |
| --- | --- |
| **2026-09-03 (목) 23:59** | 본선 제출 마감. 마감 시점 폴더 상태를 그대로 평가 |
| 2026-09-09 (수) 17:00 | 결선 진출 10팀 발표 |
| 2026-09-14 ~ 09-15 | 결선 해커톤 — **팀 전원 오프라인 참석 필수** |

제출처는 운영측이 배정하는 **Google Drive 팀 폴더**. 중간 제출 없음.
**폴더 이름을 바꾸면 그 항목은 비어 있는 것으로 처리된다.**

| 제출 폴더 | 배점 | 우리 상태 |
| --- | --- | --- |
| 01. 과제 기획서 | **50** | 🟡 본선본 초고 완성 — `제출/01_과제기획서.html` (main 에 없음, 아래 ⑦) |
| 02. 결과물 (시연영상·원본·설명서) | 25 | 🟡 프로토타입 있음, **시연 영상 없음** |
| 03. 검증 환경 | 10 | ⏳ 정답지 대조 미실시 |
| 04. 제작 과정 | 15 | ✅ 자동 생성됨 (`tools/journal.py`) |

> 심사위원은 제출된 자료만으로 판단한다. **문서에 없는 내용은 반영되지 않는다.**

**배점의 절반이 기획서다.** 코드에만 시간을 쓰면 안 된다.

---

## 3. 담당과 현재 상태

| 영역 | 담당 | 상태 |
| --- | --- | --- |
| 시나리오 20종 · 태그 53점 · 데이터 생성기 | 임도영 | ✅ |
| 검출 엔진 `engine/api.py` | 정기영 | ✅ **도착·동작** — 포함률 20/20 (예비치, #4) |
| 배선 `app/` 적재→검출→초안→승인→확정 | 박경모 | ✅ 전 구간 동작 |
| 데모 화면 4탭 | 박경모 | ✅ DCS 기준 태그 통일 완료 |
| 초안 생성 LLM | 박경모 | ⏳ 지금은 문장 틀 (Claude 미연결) |

**임시 엔진은 끝났다. 지금은 정기영님 `engine/` 이 돈다** (2026-08-25 도착).
`cli.py init` 첫 줄과 화면 오른쪽 위에 `engine` 으로 표시된다.

`python3 engine/selftest.py` → **포함률 20/20 · 알람 미발생 13/13** · 오탐 근무당 0.8건.

🔴 **이 숫자를 제출물에 그대로 쓰지 마라.** 검출기 저자가 만든 픽스처로 잰
**예비치**이고 도구 자신이 그렇게 못 박고 있다. 독립 측정은 임도영님 정답지(#5)로만
가능하며 그 파일은 아직 없다. 기획서에도 예비치로 표기했다.
화면 오른쪽 위와 `cli.py init` 출력에 `stub` / `engine` 으로 표시된다.

---

## 4. 다음 세션이 할 일 (2026-08-26 갱신 — 우선순위 순)

**전제가 크게 바뀌었다.** 기획서 초고가 완성됐고, 최대 병목이었던 검출 엔진이 도착했다.
남은 것은 대부분 **제출 폴더를 채우는 일**이다. 각 항목의 근거는 `제출_체크리스트.md` 에 있다.

1. 🔴 **데모 화면의 DCS·RTDB 를 임도영님 현행본으로 재이식** (팀장 지시)
   `demo/index.html` 의 **양식·탭 구조·스타일·③④ 탭은 그대로 두고** ① DCS 와 ② RTDB
   패널 내용만 임도영님 `docs/asu_dcs_overview.html` 기준으로 교체한다.
   패널 경계는 `demo/index.html` 에서 `class="panel"` 로 찾는다 (① 은 `panel on`).

   ⚠ **지금 데모 DCS 는 정본과 태그명이 어긋난다.** 데모는 `FT-204·FT-602·FT-710·FT-902`
   를 쓰는데 태그 마스터 정본은 `FI-` 계열이고, `AI-402·AI-703·AI-803·AI-807` 은
   데모에 아예 없다. 데모 식별자 38개 중 상당수는 태그가 아니라 설비 ID(`A-301`·`C-701`)다.
   재이식은 겉모습 교체가 아니라 **정합성 복구**다.

   ⚠ `demo/index.html` 은 6만자가 넘는다. **전체를 다시 쓰지 말고 해당 패널만 고친다**
   (CLAUDE.md 규칙). 고친 뒤 브라우저로 열어 4탭 전부 확인하고,
   화면을 고쳤으면 `python3 tools/snapshot.py` 로 `sample/` 정적본을 갱신한다.

2. **03 데이터 세트 — 조건을 바꿔가며 시험** (10점, 제출 확인 항목)
   대량 조건은 실측했다(태그 53점·12시간·114만 점). 남은 것은 **형식이 어긋난 값**과
   **빈 값**이다. 그리고 **잘 되지 않았던 조건도 적는 것이 확인 항목**이다 — 실패를
   적어도 감점하지 않고, 한계를 인식했다는 사실을 더 높게 본다.
   모사 데이터를 썼다는 사실과 방식을 적으면 원본과 동일하게 평가한다. 반드시 적는다.

3. **04 추진 계획서(WBS) 작성** (제출 확인 항목, 없음)
   ① 주차 ② 할 일 ③ 담당 ④ 시작·완료 목표 ⑤ 상태. 개발일지·회의록·난관은 자동
   생성되지만 이것만 사람이 쓴다. `git log` 와 이슈에서 실제 진행을 뽑아 쓰면 된다.

4. **시연 영상 5분 이내** (25점의 핵심, 미착수)
   배속·컷 편집을 지양한다 — **실제로 얼마나 걸리는지도 확인 대상**이다. 운영측 예시
   두 편이 2분 44초·2분 3초다. 품질·자막·음악은 평가하지 않는다.
   흐름: `bash tools/smoke.sh` + `python3 app/cli.py serve` 의 세 화면.

5. **설명서·매뉴얼** (미착수) — **개발 문서가 아니라 일반 사용자용**이다.
   ① 무엇을 위한 것인지 ② 준비물 ③ 순서대로 따라 하기 ④ **안 될 때 확인할 것**

6. **독립 포함률 측정** — `engine/selftest.py` 의 20/20 은 **예비치**다(§3 참조).
   임도영님 정답지 파일(#5)이 `data/` 에 오면 그것으로 재고, 그 숫자를 기획서 5-1 과
   03 폴더에 넣는다. 지금 기획서에는 예비치로 표기돼 있다.

7. **초안 생성 LLM 연결** — 방식은 이슈 #9 로 확정됐다. **API 키를 쓰지 않고**
   `claude` / `codex` CLI 를 `subprocess` 로 호출한다(cs-study-lms 선례).
   교체 지점은 `app/ports.py` 의 `compose()` 하나다.
   🔴 **LLM 은 반드시 선택이어야 한다.** 없으면 지금의 문장 틀로 조용히 내려가야 한다 —
   심사위원 PC 에는 로그인도 API 키도 없다. 필수로 만들면 25점과 10점을 동시에 잃는다.

8. **이슈 정리** — #4(엔진)·#7(순도)은 작업물이 도착했으나 #4 는 아직 OPEN 이다.
   확인 후 닫는다. #8(기획서 공개 여부)은 팀장 결정 대기다.


## 5. 반드시 알고 있어야 하는 함정

**① 저장소는 비공개다. 공개로 되돌리지 마라.** 규정(FAQ Q9)이 요구한다.
반면 **사이트 주소는 공개**다 — 화면에 올라가는 것은 누구나 본다.

**② 이 저장소에서 GitHub Actions 가 돌지 않는다. 원인은 확정됐다 —
계정의 Actions 분이 다른 프로젝트에서 이미 소진됐다** (팀장 확인). 공개 저장소는 분을
쓰지 않지만 비공개는 쓴다. `echo` 한 줄만 하는 워크플로도 스텝 진입 없이 즉시 실패한다.

그래서 Pages 를 **브랜치 직접 배포**로 바꿨다. `main` 에 푸시하면 GitHub 자체 빌더가
서비스한다. **워크플로 파일을 되살리지 마라 — 되살려도 안 돈다.**
CI 는 팀장이 로컬에서 돌린다(`bash tools/smoke.sh`). Actions 기반 CI 를 제안하지 마라.

**③ 근무 ID 는 날짜에서 나온다** (`YYYY-MM-DD-day`). 문서에 날짜를 하드코딩하면 하루만
지나도 깨진다. 실제로 겪었다. `--date` 로 고정하거나 `sample`·`ingest` 가 출력하는
다음 명령을 쓴다.

**④ 수치는 지어내지 않는다.** 태그·단위·한계선은 `docs/tag_master.csv`, 파형·크기·중요도는
`docs/scenarios.md` 가 정본이다. 물리적으로 불가능한 값이 두 번 나왔다
(순도 100.02%). 화면에 수치를 넣기 전에 태그 마스터 범위와 대조한다.

**⑤ 커밋 메시지가 제출물이다.** 04 폴더(15점)를 커밋과 이슈에서 자동 생성한다.
문제를 만나 고쳤으면 **본문에 적어야** 「난관과 극복 사례」로 수집된다.
조용히 고치면 그 점수에서 사라진다.

**⑦ Pages 가 `main` 전체를 공개한다. 저장소가 비공개인 것과 무관하다.**
Pages 설정이 `source: main, path: /` 라, main 에 올린 파일은 종류를 안 가리고 누구나
URL 로 읽는다. `/HANDOFF.md` `/CLAUDE.md` `/app/server.py` `/docs/제작과정/개발일지.md`
전부 익명 접근 **200** 인 것을 실측 확인했다(2026-08-25).
실명·역할은 이미 공개돼 있으나 **소속(회사·부서)은 공개 트리에 한 건도 없다.**
과제 기획서에는 소속이 들어가므로 main 에 올리면 새로 공개된다 — 그래서 `제출/` 을
`.gitignore` 에 넣고 작업 트리에만 뒀다. **공개 판단은 팀장 몫이다**(이슈 #8).
공개해도 된다고 정하면 `.gitignore` 에서 `제출/` 한 줄만 지우고 커밋하면 된다.

**⑥ 팀원 두 명(정기영·임도영)은 프로그래밍 도구에 익숙하지 않다.**
`tools/` 의 스크립트, 스모크 테스트, 문서 자동 생성은 **전부 박경모 담당**이다.
두 분에게 요구할 것은 두 가지뿐 — **자기 폴더 산출물**과 **이슈로 소통**.
팀원용 문서(`TEAM_GUIDE.md`)에 개발 도구 절차를 늘리지 마라.

---

## 6. 열려 있는 이슈

| # | 내용 | 담당 |
| --- | --- | --- |
| #2 | 검출 전처리 — 스파이크가 기준선을 오염시키지 않도록 | 정기영 |
| #3 | 시나리오별 현장 조치 사항 용도 | 미지정 |
| #4 | `engine/api.py` 검출 엔진 | 정기영 · **작업물 도착, 확인 후 닫을 것** |
| #5 | 시나리오 CSV·정답지를 `data/` 에 | 임도영 · **생성기는 됐고 파일이 아직 없다** |
| #6 | 본선 시작 안내 | 두 명 |
| ~~#7~~ | ~~순도 태그 100% 초과~~ | **CLOSED** — 임도영님이 해결 |
| #8 | 기획서를 main 에 올려도 되나 (Pages 전체 공개) | **팀장 결정 대기** |
| #9 | 초안 생성 LLM — API 대신 CLI 서브프로세스로 확정 | 결정 완료 |
| #10 | Drive 제출 안내문 팀 공유 | 두 명 |

---

## 7. 확인 방법

```bash
bash tools/smoke.sh          # 배선 전 구간 11단계 (엔진 상태로 통과 확인)
python3 engine/selftest.py   # 검출 성능 예비 점검 (제출용 수치 아님)
python3 tools/journal.py     # 04 제작과정 문서 갱신
python3 app/cli.py serve     # 화면 http://127.0.0.1:8000
```

⚠ `tools/smoke.sh` 의 `DAY` 는 `$(date +%F)` 다. **고정 날짜를 다시 박지 마라** —
원본 보관 기간(3일)을 지나면 `--redo` 단계가 깨지고, 그것을 엔진 문제로 오진하게 된다
(2026-08-26 실제로 겪었다).

**Drive 제출 폴더 접근** — Chrome 확장 대신 `rclone` 로 붙였다. 세션과 무관하게 동작한다.
```bash
rclone lsf gdrive: --drive-root-folder-id 1XyKabi7L6FcREAhKWZgJz52nGSgXTMdR -R
```
제출 폴더의 안내문 요구사항은 `제출_체크리스트.md` 에 정리해 두었다. **폴더 이름을 바꾸지 마라.**

🟢 **실제 앱 (제출용 · 조작 가능)** — https://engra.64-176-227-85.sslip.io
`/` 일지 조회 · `/shift/<근무ID>` 초안 검토·확정 일지 · `/api/shifts` · `/api/draft/<근무ID>`
Vultr + Caddy(자동 HTTPS) + systemd. `engra.service` 가 `127.0.0.1:4320` 에서 돌고
Caddy 가 프록시한다. 같은 서버의 cs-study-lms 와 별도 유저·별도 포트로 격리돼 있다.
**시드는 근무 3건**(2건 확정 + 1건 승인 대기)이고 `engra-reset.timer` 가 매시 초기
상태로 되돌린다 — 방문자가 승인을 눌러도 다음 방문자가 같은 흐름을 본다.
⚠ 시드를 다시 만들 때는 **근무별로 `ingest → run → approve` 순서**로 돌려야 한다.
셋을 다 `run` 한 뒤 승인하면 과거 조치가 회수되지 않는다(사례 검색이 초안 생성
시점에 일어난다 — 2026-08-26 실제로 겪었다).

정적 배포 화면 (백업 경로 · 앱이 죽어도 볼 수 있다)
- 데모 4탭 https://ssmo0810.github.io/engra/demo/
- 프로토타입 스냅샷 https://ssmo0810.github.io/engra/sample/
- 소개 https://ssmo0810.github.io/engra

`sample/` 은 서버가 만든 화면을 그대로 뜬 정적본이다. **화면(`app/server.py`)을 고쳤으면
`python3 tools/snapshot.py` 를 돌려야 갱신된다.** 서버를 띄워 세 화면을 받아 정적으로
고친 뒤 내리는 것까지 스크립트가 다 한다.
