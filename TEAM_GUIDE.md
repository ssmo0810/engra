# 팀 작업 가이드

앙그라쥬 팀원이 이 저장소를 함께 수정하고 관리하기 위한 안내입니다.
개발 경험이 없어도 웹 브라우저만으로 수정할 수 있습니다.

---

## 1. 이 저장소가 하는 일

여기에 올린 파일이 **그대로 웹사이트가 됩니다.** 파일을 고치고 저장하면 1~2분 뒤 실제 주소에 반영됩니다.

| 파일 | 내용 | 주소 |
| --- | --- | --- |
| `index.html` | 서비스 소개 페이지 | https://ssmo0810.github.io/engra |
| `demo/index.html` | 화면 데모 (DCS · RTDB · 초안 검토 · 일지 조회) | https://ssmo0810.github.io/engra/demo/ |
| `README.md` | 저장소 첫 화면 설명 | 저장소 메인에 표시 |
| `.github/workflows/pages.yml` | 자동 배포 설정 | — |
| `.nojekyll` | 배포 설정 (빈 파일, 지우지 말 것) | — |
| `docs/scenarios.md` | 특이 시나리오 정의 (임도영 담당) | — |
| `CLAUDE.md` | Claude Code 작업 규칙 | — |

---

## 2. 초대 수락하기

1. 초대 메일이 GitHub 가입 주소로 갑니다. 메일 안의 **Accept invitation** 을 누르세요.
2. 메일이 안 보이면 https://github.com/ssmo0810/engra 에 접속하면 화면 위쪽에 초대 배너가 뜹니다.
3. 수락하면 파일을 직접 수정할 수 있는 권한(Write)이 생깁니다.

GitHub 계정이 없다면 https://github.com/signup 에서 먼저 가입하고, 사용자명을 팀장에게 알려주세요.

---

## 3. 수정 방법 ① — 웹 브라우저에서 (권장)

가장 간단하고 안전한 방법입니다. 설치할 것이 없습니다.

1. https://github.com/ssmo0810/engra 접속
2. 수정할 파일을 클릭 (예: `README.md`)
3. 오른쪽 위 **연필 아이콘(Edit this file)** 클릭
4. 내용을 고친다
5. 아래 **Commit changes** 클릭
   - 위 칸에 **무엇을 바꿨는지 한 줄**로 적는다 (예: `데모 설명 문구 수정`)
   - **Commit directly to the main branch** 선택 → **Commit changes**

끝입니다. 1~2분 뒤 사이트에 반영됩니다.

> 파일을 새로 올리려면 저장소 메인에서 **Add file → Upload files** 를 쓰면 됩니다.

---

## 4. 수정 방법 ② — 내 컴퓨터에서

HTML을 크게 고치거나 여러 파일을 한꺼번에 다룰 때 편합니다.

**GitHub Desktop 사용 (개발 도구가 낯선 경우 권장)**

1. https://desktop.github.com 에서 설치하고 GitHub 계정으로 로그인
2. **File → Clone repository → ssmo0810/engra** 선택
3. 내 컴퓨터의 파일을 평소처럼 편집기로 수정
4. GitHub Desktop 화면에서 변경 내용 확인 → 아래에 설명 한 줄 입력 → **Commit to main**
5. 오른쪽 위 **Push origin** 클릭 → 사이트에 반영

**터미널 사용**

```bash
git clone https://github.com/ssmo0810/engra.git
cd engra
# 파일 수정 후
git pull                     # 남이 바꾼 내용 먼저 받아오기
git add 수정한파일
git commit -m "무엇을 바꿨는지"
git push
```

---

## 5. 자동 배포가 어떻게 되나

`main` 브랜치에 저장(commit)되면 GitHub Actions가 자동으로 사이트를 다시 만듭니다.

- 진행 상황은 저장소 상단 **Actions** 탭에서 볼 수 있습니다.
- 초록색 체크 표시가 뜨면 반영 완료입니다 (보통 1~2분).
- 사이트가 그대로면 브라우저 강력 새로고침(`Ctrl+Shift+R` / `Cmd+Shift+R`)을 해보세요. 캐시 때문인 경우가 많습니다.

---

## 6. 같이 작업할 때 규칙

여러 명이 같은 파일을 동시에 고치면 충돌이 납니다. 아래만 지키면 대부분 예방됩니다.

- **수정 전에 카톡 등으로 "나 지금 데모 화면 고칠게" 한마디** — 가장 확실한 예방책입니다.
- **내 컴퓨터에서 작업한다면 시작 전에 반드시 `git pull`** (GitHub Desktop은 `Fetch origin`).
- **한 번에 한 가지만 고치고 바로 저장** — 오래 붙잡고 있을수록 충돌 위험이 커집니다.
- **커밋 설명은 한 줄이라도 남기기** — 나중에 누가 무엇을 바꿨는지 추적할 수 있습니다.

큰 변경이라 검토를 받고 싶다면, 웹 편집 화면에서 저장할 때 **Create a new branch ... and start a pull request** 를 선택하면 팀원이 보고 승인한 뒤 반영됩니다.

---

## 7. Claude Code로 작업할 때

세 사람이 각자 Claude Code로 같은 저장소를 만지면 서로의 작업을 덮어쓸 수 있습니다. 저장소 안의 `CLAUDE.md` 에 규칙을 넣어두었고, **Claude Code는 저장소를 열 때 이 파일을 자동으로 읽습니다.** 따라서 대부분은 알아서 지켜집니다. 사람이 챙길 것은 아래 셋입니다.

- **작업 시작할 때 "먼저 git pull 하고 시작해줘" 라고 말하기.** 이것만으로 충돌의 대부분이 예방됩니다.
- **자기 담당 파일만 건드리기.** 담당 구분은 `CLAUDE.md` 표에 있습니다. 남의 파일을 고쳐야 하면 먼저 알리세요.
- **한 가지 끝나면 바로 커밋·푸시.** 오래 붙잡고 있을수록 남의 작업과 어긋납니다.

푸시할 때 `rejected` 또는 `non-fast-forward` 라는 메시지가 나오면, 내가 작업하는 동안 다른 사람이 먼저 올린 것입니다. 당황하지 말고 Claude Code에 이렇게 말하면 됩니다.

> "git pull --rebase 하고 충돌 있으면 정리한 다음 다시 push 해줘"

`--force` 로 밀어붙이는 것은 남의 작업을 지웁니다. 절대 하지 마세요. (`CLAUDE.md` 에도 금지로 적혀 있습니다.)

---

## 8. 실수했을 때 되돌리기

파일이 망가져도 **모든 이전 버전이 남아 있으므로 항상 되돌릴 수 있습니다.**

1. 저장소 상단 **Commits** (또는 파일별 **History**) 클릭
2. 되돌리고 싶은 변경 항목을 연다
3. 오른쪽 위 **... → Revert** 클릭 → 저장

즉시 이전 상태로 돌아가고, 사이트도 다시 배포됩니다.

---

## 9. 절대 올리면 안 되는 것

이 저장소는 **공개(Public)** 이며 누구나 볼 수 있습니다. 해커톤 보안 규정에 따라 아래는 업로드 금지입니다.

- 사내 기밀 자료 (영업비밀, 미공개 경영정보, 기술 사양서, 내부 전략 문서)
- 개인정보 (성명, 사번, 연락처, 이메일 등 개인을 식별할 수 있는 정보)
- 사내 시스템에서 추출한 원본 데이터, 로그, 데이터베이스 덤프
- 실제 설비 태그 체계, 실제 운전 수치, 사내 시스템 화면 캡처

**현재 데모의 모든 태그명·수치·일지는 가상으로 만든 값입니다.** 화면이나 문구를 수정할 때도 실제 값을 넣지 마세요.
판단이 애매하면 올리기 전에 팀장에게 먼저 확인하고, 필요하면 소속 멤버사 정보보호 담당 부서에 문의합니다.

---

## 10. 막혔을 때

- 저장소 상단 **Issues → New issue** 에 남기면 팀원 모두가 봅니다.
- 급한 경우 팀 채팅으로 알려주세요.
