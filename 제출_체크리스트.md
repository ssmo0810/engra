# 제출 체크리스트 — 마감 2026-09-03(목) 23:59

> **본선 마감 문서(09-03)** — 결선 제출은 발표자료 1종(09-15 07:00 팀 Drive · [결선_실행계획.md](결선_실행계획.md)). 아래는 본선 제출 때의 기록이다.

Drive 팀 폴더의 안내 문서 8종(README_00 ~ README_04 및 02 하위 3종)을 읽고,
**우리가 무엇을 해야 하는가**로 정리한 것이다. 원문 전재가 아니다 —
Ground Rule 이 해커톤 정보의 무단 공유를 실격 사유로 두고 있어 요구사항만 우리 말로 옮겼다.
원문은 Drive 폴더에 그대로 있으니 판단이 애매하면 거기서 확인한다.

**마감 시점의 폴더 상태가 그대로 평가된다.** 중간 제출 없고, 마감 후 추가 제출·심사위원 소통 불가.

---

## 폴더 이름 주의 — 03 은 「데이터 세트」다

허브의 제출 방법 문서에는 `03. 검증 환경` 으로 적혀 있으나 **실제 Drive 폴더는 `03. 데이터 세트`** 다
(2026-08-24 에 이름이 바뀐 흔적). 평가는 실제 폴더 구조를 읽어가므로 **Drive 쪽이 기준**이다.
폴더 이름은 바꾸지 않는다 — 이름이 다르면 그 항목은 비어 있는 것으로 처리된다.

```
01. 과제 기획서/          02. 결과물/                   03. 데이터 세트/    04. 제작 과정/
                          ├ 1. 시연 영상/
                          ├ 2. 결과물 원본/
                          └ 3. 설명서·매뉴얼/
```

공통: 양식 자유 · 파일명 자유(`과제기획서_팀명.pdf` 권장) · **분량과 디자인 완성도는 평가 대상이 아니다.**

---

## 01. 과제 기획서 — 50점

**상태: rev.2 로 교체(09-01 임도영 결정).** 정본은 `docs/과제기획서_rev2.html` (20쪽 — 표지 포스터·시나리오 24종/측정 20종 구분·AI 실측 표·50일/848태그 시험·QA 13회)이고 `refresh_submission.sh` ⑤ 가 렌더해 `제출/01. 과제 기획서/과제기획서_앙그라쥬.pdf` 를 덮는다. 이전 판(08-28, 19쪽) — 수정 후 재측정 수치(100%/100%+추적 1·오탐 1.0/1.8 — 복귀 제외 0/0)·빈 상태 시작·관리 페이지·AI 조치 미작성·사용자 QA 난관 ③ 반영. 정상 데이터(#17) 반영 완료. 08-31 재확인 — 기획서 본문 수치가 `docs/공식수치.md` 와 일치

심사는 다섯 개 판정 질문으로 본다. 각 질문에 우리 문서가 답하는 위치를 적었다.

| 판정 질문 | 답하는 곳 | 상태 |
| --- | --- | --- |
| 문제가 구체적 데이터·근거에 기반해 정의됐나 | 2-1 · 2-2 (시나리오 20종 중 알람 7종, 태그 53점) | ✅ |
| 기대 성과가 정량 목표로 제시되고 **산출 근거가 타당한가** | 5-1 (근거 열 신설) | ✅ |
| 업무·조직·프로세스로 확장 가능성이 구체적인가 | 6-3 임팩트 범위 | ✅ |
| AS-IS 대비 TO-BE 차이가 명확하고 개선폭이 유의미한가 | 2-3 · 3-4 | ✅ |
| AI 가 업무의 핵심 의사결정·실행에 관여하는 수준인가 | 3-5 AI 네이티브 — **4개 판단 계층 실측 표** (중요도·사례·묶음·서술) | ✅ |

제출 전 확인 항목에 "목표 수치와 그 산출 근거가 함께 적혀 있다" 가 있어, 5-1 표에
**「그 값을 정한 근거」열을 신설**해 목표 7개 전부에 근거를 붙였다. 예: 포함률 90% =
20종 중 18종 = 알람 미발생 13종 전부 + 알람 발생 7종 중 5종. 5분 = 교대 1시간 전
생성이라는 운영 조건의 10%. 100%를 걸지 않은 이유(원리적으로 단일 태그 검출 불가한
유형이 시나리오에 포함됨)도 함께 적었다.

- [x] 본선 진행 내용 반영 (예선본 재업로드 아님 — 예선 양식은 유지) — `제출/01. 과제 기획서/과제기획서_앙그라쥬.pdf`
- [x] 02-2 결과물 원본: 접속 주소 문서 + **소스 스냅샷 zip**(저장소가 비공개라 운영진 안내 없이도 열 수 있게, .env 등 비밀 없음)
- [x] 목표 수치의 산출 근거 명시
- [x] 사번·성명·거래처·금액 등 민감 정보 없음

---

## 02. 결과물 — 25점

판정 질문: **사용자가 오류·결함·속도·사용성으로 막히지 않고 결과 도출까지 도달할 수 있는가.**

### 1. 시연 영상 — ✅ **09-01 교체본** (`제출/02-1_시연영상_앙그라쥬.mp4`) — 4분 34초 · 0:00~6:52 구간 1.5배속(화면 표시) · 컷 편집 없음 · 대기 구간에는 AI 서술 호출이 돈다
교체 전 64초판(1440×900)의 흐름 — 이력: DCS → RTDB → 초안 검토(15항목, 2건 제외, 코멘트 입력) → 승인 → 일지 조회.
(이력 메모) ⚠ **AI 끈 상태로 녹화한 초안.** 서버가 이제 `AI cli` 로 8근무를 시드 중 → 끝나면 **서버 DB 를 로컬로 받아 재녹화** (트렌드·중요도 판단·사례 판정·함께 봐야 할 항목·재검토까지 한 흐름으로). 절차 HANDOFF §4-4.

- 5분 이내. **배속·컷 편집 지양** — 실제로 얼마나 걸리는지도 확인 대상이다.
  5분을 넘기면 대기 구간만 배속하고 화면에 배속 표시를 남긴다.
- **품질·심미성·자막·음악은 평가 대상이 아니다.** 화면 녹화 그대로 충분하다.
- 담을 것 — ① 누가 어떤 상황에서 쓰기 시작하는지 ② 어떤 화면·기능을 따라가는지
  ③ 어떤 결과가 나와 문제가 어떻게 풀리는지 ④ 어떤 데이터·환경에서 실행한 것인지
- 운영측 예시 두 편이 각 2분 44초 / 2분 3초다. **짧아도 된다.**
- 우리 시나리오: `bash tools/smoke.sh` 흐름 + `python3 app/cli.py serve` 화면 3탭

### 2. 결과물 원본

적어 올릴 것 — ① 접속 주소 ② 외부에서 열리는지 ③ 안 열리면 그 사정 ④ 저장소 주소와 **공개 설정**

- 🟢 **작동하는 앱 (조작 가능)** — `https://engra.64-176-227-85.sslip.io` · 외부 익명 접근 **200 확인**
  심사위원이 직접 승인 버튼을 눌러 확정 일지가 만들어지는 것까지 볼 수 있다.
  근무 3건 시드(2건 확정 + 1건 승인 대기), 상태는 매시 초기화된다.
- 정적 배포 3개도 익명 접근 200 (소개 · 데모 · 프로토타입 스냅샷) — 앱이 죽었을 때의 백업 경로
- 저장소는 **비공개**다. 규정이 요구한다. 공개로 되돌리지 않는다.
  심사위원 열람이 필요하면 운영진이 별도 안내한다 — 우리가 임의로 초대하지 않는다.

### 3. 설명서·매뉴얼 — ✅ 작성

**개발 문서가 아니라 일반 사용자용 문서다.** 개발자가 아니어도 따라할 수 있어야 한다.
① 무엇을 위한 솔루션인지 ② 쓰기 전에 준비할 것 ③ 순서대로 따라 하는 사용법
④ **잘 동작하지 않을 때 확인할 사항**

- [x] 시연 영상 — 09-01 교체본 4분 34초 · 0:00~6:52 구간 1.5배속(화면 표시) (교체 전 64초판 흐름 — 이력: DCS→RTDB→초안: 트렌드·중요도 판단·과거조치·묶음→중요도 변경·2건 제외·코멘트→승인→확정→일지)
- [x] QA 순서 문서 — `제출/QA_순서_앙그라쥬.md`
- [x] 결과물 원본 + 링크·공개설정 문서 — **rev.2 로 교체(09-01)**: 정본 `docs/결과물원본_rev2.html` (3쪽 — 주소·외부 접근 200 실측·안 열릴 때·공개 설정). 이전 md 판과 문장 대조 미실시, 교체 전 확인
- [x] 사용자용 설명서 — **rev.2 로 교체(09-01)**: 정본 `docs/설명서_rev2.html` (4쪽 — 무엇·준비물·따라 하기 6단계·안 될 때 8가지). 이전 md 판과 문장 대조 미실시, 교체 전 확인
- [x] 저장소 비공개
- [x] 적은 주소가 실제로 열리는 것 확인 (09-01 · 배포 후 전 페이지 200)

---

## 03. 데이터 세트 — 10점

판정 질문: **결과물이 실제 업무 데이터 또는 실제 사용 환경과 가장 유사한 환경에서 작동하는 것이 확인되었는가.**
「실제 업무 데이터」는 원본을 올리라는 뜻이 아니다 — **유사한 구조·같은 난이도**로 확인했는지를 본다.

올리는 것: ① 학습·검증에 쓴 데이터 세트 파일(CSV 등) ② 동작 확인 자료(화면 캡처·로그·결과 파일)

⚠ **여기가 우리 계획과 가장 크게 다르다.** 단순히 "검증했다"가 아니라 **조건을 바꿔가며 시험한 기록**을 요구한다.
조건 예시로 데이터가 많을 때 · 형식이 어긋난 값이 섞였을 때 · 값이 비어 있을 때를 들고 있다.

그리고 **"잘 되지 않았던 조건도 함께 적었다"가 제출 전 확인 항목이다.**
실패 조건을 적어도 감점하지 않는다 — 한계를 인식하고 검증했다는 사실을 더 중요하게 본다.
한 가지 조건만 확인하고 끝내지 말라고 명시돼 있다.

현재 상태와 할 일:

| 조건 | 상태 |
| --- | --- |
| 데이터가 많을 때 | ✅ 태그 53점 · 12시간 · 114만 점에서 적재 3.2초 + 검출·초안 2.2초 |
| 검출 엔진 통합 | ✅ `engine/` 도착·동작. 스모크 11단계 엔진 상태로 전 단계 통과 |
| 자체 점검 포함률 | ✅ 예비치 20/20 · 알람 미발생 13/13 · 오탐 근무당 0.8건 (독립 측정 아님 — 자체 픽스처 이력, 정본은 `docs/공식수치.md` ①②) |
| 물리적으로 불가능한 값 (순도 100% 초과) | ✅ 해결 — 생성기에 물리 여유 클램프·하향 편향 진동 도입 (#7, 임도영) |
| 형식이 어긋난 값이 섞였을 때 | ✅ 기본=첫 오류에서 멈춤(줄번호) / `--skip-bad-rows`=546행 건너뜀·보고·1% 초과 거부. **조용한 오염 1건 발견**(천단위 콤마) |
| 값이 비어 있을 때 | ✅ 5,724행 빈 값 + 2h 결측 — 건너뜀, 결측 경계 아티팩트 없음 |
| 정답지 대조 포함률 | ✅ **독립 측정** — 2근무 **22/22(100%)**·20/20종 / 6근무 **46/46(100%)**·21/21종. `tools/score.py` (08-26 첫 측정 21/22·47/48 은 이력 — 정본은 `docs/공식수치.md` ①②) |
| 오탐 | ✅ **근무당 1.0 / 1.8건 — 전부 복귀 구간, 복귀 제외 0건** (08-26 첫 측정 10~11건은 외기 일주기 오인, #13 으로 수정. 잘 안 된 조건으로 기록) |
| 놓친 시나리오 | ✅ 없음 — 미탐지 0건. (08-26 첫 측정에서 #2 토출온도 드리프트 미검출 → #13 수정 · 「추적 중」 채점 정의로 해결) |
| 모사 데이터를 쓴 사실과 방식 | ✅ `제출/03_검증결과_앙그라쥬.md` 2절 |
| 순도 태그 상한 준수 | ✅ `AI-707`·`AI-803` 최대값이 정상 상한 이내 (생성기 수정 반영) |

**모사 데이터를 썼다는 사실과 그 방식을 검증 결과 정리에 적으면 원본 데이터와 동일하게 평가한다.**
반드시 적는다. 태그 마스터·시나리오 정의가 얼마나 실제와 유사하게 구성됐는지도 함께 밝힌다.

- [x] 검증 결과 정리 문서 — **rev.2 로 교체(09-01)**: 정본 `docs/검증결과정리_rev2.html` (모사 방식·조건 8종 표·재현 명령·잘 안 된 5건). `refresh_submission.sh` ⑤ 가 렌더. 이전 md 판과 문장 대조 미실시
- [x] **데이터 세트 파일** — `03. 데이터 세트/데이터세트/`: 기준 2근무 CSV+정답지, sim 6근무 CSV+정답지, tag_master.csv, scenarios.md, README (286MB) — 08-28 추가(전엔 Pages 링크만)
- [x] **동작 확인 자료** — `03. 데이터 세트/동작확인자료/`: 화면 캡처 8장(①~⑤·관리·정답지 뷰), 채점 출력 2건, 스모크 출력, 서버 파이프라인 로그(경모님 QA), 태그 연결 추정 CSV·요약 — 08-28 추가
- [x] 확인한 조건과 그 결과 (대량·형식오류·빈값·결측·독립채점·AI)
- [x] 잘 되지 않았던 조건도 기록 (5건)
- [x] 사내 원본 데이터 없음 — 전부 가상

---

## 04. 제작 과정 — 15점

판정 질문: **난관과 어려움에 굴복하지 않고 끈기있게 도전하여 문제를 해결했는가.**

네 가지를 요구하고, 그 중 셋이 제출 전 확인 항목이다.

| 항목 | 상태 |
| --- | --- |
| 추진 계획서 (WBS 또는 준하는 것) | ✅ `제출/04. 제작 과정/추진계획서_앙그라쥬.md(+pdf)` |
| 개발 일지 | ✅ `제출/04. 제작 과정/개발일지_앙그라쥬.md(+pdf)` — 커밋에서 생성, **제출 폴더에 복사**(08-28; 전엔 저장소 안에만 있었음 — 경모님 지적) |
| 회의록 | ✅ `제출/04. 제작 과정/회의록_앙그라쥬.md(+pdf)` — 이슈 18건에서 생성 |
| 난관과 극복 사례 | ✅ `제출/04. 제작 과정/난관과극복사례_앙그라쥬.md(+pdf)` — 커밋 본문 + 팀원 직접 기록(#15·engine/난관기록.md) |
| (추가) 사용 도구 | ✅ `사용도구_앙그라쥬.md` — FAQ Q5 |

추진 계획서에 담을 것 — ① 주차 ② 할 일 ③ 담당 ④ 시작·완료 목표 ⑤ 상태

⚠ **난관 서술에 요구되는 다섯 요소**가 있다. 우리 자동 생성물이 이걸 다 담는지 확인해야 한다.
① 무엇이 예상과 달랐는지 ② 넘기 위해 무엇을 시도했는지 **순서대로** ③ 각 시도의 결과
④ 최종적으로 무엇을 남기고 무엇을 포기했는지와 그 이유 ⑤ 그 과정에서 새로 알게 된 것

→ 커밋 본문을 쓸 때 **④ 포기한 것과 ⑤ 새로 알게 된 것**을 의식적으로 남긴다. 이 둘이 가장 빠지기 쉽다.
**실패한 시도도 그대로 적는다.** 글솜씨를 평가하지 않는다.

- [x] 추진 계획서 작성 — `제출/04_추진계획서_앙그라쥬.md`
- [x] 개발 일지 · 회의록 · 난관 사례 (제출 직전 `python3 tools/journal.py` 재생성)
- [x] 민감 정보 없음

---

## 제출 직전 공통 점검

- [x] 01~04 네 폴더에 요구 자료가 다 있다 — 로컬 `제출/` 가 Drive 구조를 미러(08-28). Drive 업로드는 `bash tools/submit_drive.sh go`(마지막 날)
- [x] 라이브 심사 상태 세팅 (09-01) — 확정 3건(08-29-day·08-29-night·08-30-day) + 승인 대기 1건(08-30-night). 대기 초안 6항목 중 **4항목에 「과거 조치 — 확정 일지 N건」** 이 붙은 것을 화면에서 확인
- [x] `refresh_submission.sh` → `submit_drive.sh go video` 실행 (09-01) — **지금 시점으로 제출 가능 상태.** 09-02~03 에 커밋을 더 하면 한 번 더 돌린다(수치는 sync_counts 가 맞춘다)
- [x] 02-1 시연 영상 교체 (09-01) — 4분 34초(규정 5분 이내) · 0:00~6:52 구간 1.5배속 · 규정대로 화면에 「1.5배속」 표시
- [x] 저장소가 비공개다 (08-31 `gh repo view` 실측 — PRIVATE)
- [x] 사내 원본 데이터·계정·비밀번호가 없다 (08-31 제출 폴더 전수 스캔 — 모사 데이터임을 기획서·데이터세트 README 에 명시)
- [x] 사번·성명·거래처·금액 등 민감 정보가 없다 (08-31 스캔 — 「사번」 1건은 규정 인용문, 실제 식별자·이메일·전화 0건)
- [x] 적어 올린 주소가 실제로 열린다 — 다른 환경 확인 완료 (09-01 경모님 휴대폰에서 접속 확인)
- [x] `python3 tools/journal.py` 로 04 문서를 최신화했다 (08-31 · 이제 `tools/refresh_submission.sh` 가 대신 돌린다)

## 참고

- 팀원의 **이름·소속·역할은 개인정보가 아니며 제출물에 밝혀야 한다.** 가려야 하는 것은 제3자 정보다.
- 세미나·OT 참석 여부는 평가에 반영되지 않는다.
- Claude 팀플랜 = 1인 1계정, 프리미엄 시트(Max 5x 와 동일 크레딧). LLM API 비용은 팀당 최대 5만원 실비 지원.
- 본선 결과물이 결선의 출발점이다. 심사평은 9/14 결선 당일 공개된다.
