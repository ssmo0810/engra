# 결선 실행 계획 — ENGRA (2026-09-14 결선 D1)

> 제출: **9/15(화) 07:00 팀 Drive 「254. 앙그라쥬」에 발표자료(HTML 로컬 파일) → 카톡방 「제출 완료」**. 07:00 이후 추가 개발 금지. 발표 10분 + Q&A 5분, **개별 팀 PC**. 발표·시연: 임도영 프로.
> 심사: 5인(AX사업총괄 · 벤처캐피탈 심사역 · AI사업부문 대표 · 교수 2) — **「심사의 핵심 키워드는 투자」**. 3대 기준: ① 임팩트(해결 전·후를 수치로, 확장 개인<팀<전사<사회) ② AI 네이티브 ③ 완성도(실사용 조건·재현 가능성·오류 없음).
> 원칙: 심사평을 다 따르지 않는다 · 엔진(`engine/`) 무수정 · 팀원과 겹치지 않게 격리 브랜치 → PR 로 main · **작업은 계속 GitHub 에 push** · 조용한 폴백 금지 · 수치는 `docs/공식수치.md` 하나에서만.

---

## 0. 확정된 결정

| 항목 | 결정 |
|---|---|
| 속도 개선 방식 | 항목마다 AI 호출 1개를 **동시 실행** + 끝나면 **연속성 AI 1회**(근무 전체 범위로 같은 사건 묶기·중복 표시) |
| 사람 기록 표준화 | 채택 시 상태 **완료 / 진행중** 두 개, 기본값 없음(안 고르면 승인 불가 — 서버에서 막음) |
| 이월 | 진행중 항목은 다음 근무 초안 맨 위 **접힌 「이월 항목 N건」 묶음**으로 따로. 완료로 닫을 때까지 유지 |
| 운전모드·정비·센서 불량 | 엔진 수정 없이 **프롬프트**로. 연결 태그 상태를 넘겨 AI 가 「단독 이상(계측·격리·정비) 의심」 판정 — 단정 아님 |
| 과거 사례 증상 검색 | **안 함** |
| 발표자료 | **HTML 단일 로컬 파일**(외부 링크 없음), 저장소 SK 빨강 양식 |
| 데모 사이트 | 계속 운영. 시연 방식(라이브 vs 캡처)은 속도 결과 보고 결정 — 병렬화로 초안 생성까지 라이브 가능하면 라이브 |
| 데모용 승인 대기 근무 | 뒤로 미룸(초안 생성 자체를 시연에서 보이는 쪽 검토) |
| 공식수치 갱신 | 경모님 확인 → GitHub 반영 |
| 문서·GitHub 최신화 | 전체 진행 |
| main 반영 | 격리 시험 → **PR** → 머지 |

---

## 1. 풀 문제 3개 (화이트보드)

1. **AI 초안 생성 속도** — 순차 5개 묶음(16항목 5분 42초, 정지 근무 추정 20분) → 항목별 병렬 + 연속성 1회, **같은 근무로 전후 실측** (심사평 01·05·06·07)
2. **사람 기록 표준화 + 실사용 첫 실측** — 완료/진행중 스위치·이월 묶음, 채택률(임도영 프로 운전원 조사) (심사평 03·04·06·08)
3. **운전모드·단독 이상 맥락을 AI 판정으로** — 정지 확인 질문(기존) + 연결 태그 상태 기반 단독 이상 의심(엔진 무수정) (심사평 05)

---

## 2. 작업 흐름과 파일 경계 (서로 안 겹침)

| 흐름 | 브랜치 | 만지는 파일 | 상태(14:10 기준) |
|---|---|---|---|
| A. AI 서술 병렬화 | `final/speed-parallel` | `app/llm.py` · `app/pipeline.py`(호출부) · `tests/` | 구현 중(한도로 멈췄다 재개) |
| B. 상태 스위치·이월 | `final/status-switch` | `app/db.py` · `app/approve.py` · `app/server.py` · `tests/` | 커밋 2개 push(9d2aca7) · codex 반증 대기 |
| C. 단독 이상 프롬프트 | (A 머지 뒤 `final/isolated-prompt`) | `app/llm.py`(프롬프트·스키마) · 조립 함수 | **가상 시험 끝 — 결과 §4** |
| D. 이월 조립 훅 | (A·B 머지 뒤 `final/carry-hook`) | `app/pipeline.py` | 대기 |
| E. 문서 정본·최신화 | `final/docs-sync` | `docs/공식수치.md` · scenarios · rev2 HTML · README · HANDOFF · status | 진행 중(한도로 멈췄다 재개) |
| F. 발표자료 HTML | `final/slides-html` | `docs/결선/발표자료.html` | 뼈대 작성·codex 반증 중 |
| G. Q&A 예상 질문 v2 | `final/qa-update` | `docs/결선/QA_예상질문_v2.md` | **44문 완료(986b9bb)** · 상태 2개로 정정 중 |
| H. 속도 규모 곡선 | `probe/probe-speed` | `tmp_speed/`(조사) | 530태그까지 측정, 병렬 분할 시험 중 |

**병합 순서:** B·A(파일 안 겹침, 각자 PR) → C(A 위에) → D(A·B 위에) → E·F·G(문서, 마지막에 수치 채움) → 수치 확정 후 F 의 `[값 대기]` 채움.

---

## 3. 속도 설계 — 두 층

### 3-1. AI 서술 (지금 병목)
- **1단계:** 항목 1개 = 호출 1개, 동시 상한 `ENGRA_LLM_PARALLEL`(기본 8). 항목별 재시도 2회, 하나라도 끝내 실패하면 멈춤(폴백 금지).
- **2단계(연속성):** 전 항목의 태그·제목·중요도·시각·근거 요약을 한 번에 → `related_idx`(근무 전체) · `duplicate_of`. 실패 시 1단계 결과는 살리고 「연속성 판정 실패」를 화면에 명시.
- 정지 확인 질문(`origin=question`)·원본 품질 항목(`origin=quality`) 특례 유지.
- **실측:** 같은 근무(일반 6~16항목 · 정지 30건+)로 기존 순차 vs 새 병렬, 각 2회 → 공식수치 ⑧.

### 3-2. 검출 (규모가 커질 때의 병목) — 실측 근거
- 53태그 · 12시간: 적재 3.2초 + 검출·초안 틀 2.2초 (기획서 rev2)
- 530태그(10배): **적재 22~24초 + 검출 10.8~11.0초**, 두 번 같음 (probe-speed, 합성 복제 데이터)
- 848태그(16배): **검출 108.3초 · 대략 n^1.3** — 제곱 아님, 상관 검출이 태그 마스터 연결 쌍만 봄. **메모리가 진짜 제약**(848×21,600점×8바이트 ≈ 146MB + 블록·뷰) (docs/scenarios.md 「태그를 16배로 늘려도 검출은 108초다」)
- 적재 비용은 행 단위 파이썬 처리(csv 한 줄씩 · 시각 변환)와 SQLite 쓰기에 몰려 있음 (probe-speed 프로파일)
- **설계 방향(태그 1,000개·억 단위):** 태그를 연결 그룹 단위로 나눠 워커별 독립 검출(연결 쌍이 그룹 경계를 넘지 않게 — 연속성 유지) → 결과 합치기 · 적재는 열 단위 일괄 변환 · 원본은 근무 창만 메모리에. **태그 분할 병렬이 순차와 같은 이벤트를 내는지 530태그에서 시험 중**(probe-speed) — 결과 오면 이 절 갱신.
- 발표에서는 규모 의존 절대 시간(114만 점·5.4초)은 빼고, **「태그 수에 대략 n^1.3, 그룹 분할 병렬로 줄이는 구조」**와 AI 전후 배수로 말한다.

---

## 4. 단독 이상 AI 판정 — 가상 시험 결과 (probe-isolated, 원문 확인)

**데이터:** 생성기로 고착(#7 ZI-801 · 진짜 밸브 고착) · 서지 전조(#8) · 순도 동반 저하(#15) + **LI-806(LIN 레벨) 09:00~11:00 수동 동결**(연결된 ZI-805 개도는 원본 그대로 = 순수 계측기 고착). 엔진 이벤트 13 → 초안 4항목.

| 항목 | A: 지금 프롬프트 (2회) | B: 연결 태그 상태 + `isolated_suspect` (2회) |
|---|---|---|
| [0] AI-707 순도 드리프트 | 상 · 전달 | 상 · 전달 · **false** |
| [1] **LI-806 계측기 고착(시험 대상)** | 중/상 흔들림 · ZI-801 과 **잘못 묶음(2/2)** | 중 · 전달 · **true 2/2** · 「연결 태그 ZI-805 가 37~39.8% 에서 정상 변동하는데 LI-806 만 120분 82.0% 고착 — 계측기 고착·격리 의심」 |
| [2] FI-204 서지 전조 | 상 · 전달 | 상 · 전달 · **false** |
| [3] ZI-801 진짜 밸브 고착(대조군) | 상 · 전달 | 상 · 전달 · **false** — 「LOX 레벨 이탈 중 밸브 무반응 = 밸브 고착」 |

- **대조군 오판 0.** 계측기 고착과 진짜 공정 이상을 갈랐고, A 의 잘못 묶음이 B 에서 사라짐. 잔여 오류: B 2회차에 [0]↔[3] 약한 묶음 1건.
- **엔진 수정 불필요.** 조립 재료 = `engine.api._neighbours(tag)` ∪ 항목 `related_tags`, 같은 구간 겹치는 이벤트가 있으면 「이상(종류)」, 없으면 구간 min/max 가 정상범위 안이고 변동이 있으면 「정상(범위 안·변동 있음, a~b)」.
- **함정:** ① `_neighbours` 만 쓰면 함께 오른 태그가 빠져 「모두 정상」 오판 직전(드라이런 실증) → 합집합 필수 ② 연결 태그 없는 태그 많음 → 「연결 태그 없음 — 단독 판단 불가」 + false 고정 ③ 결측 구간은 미시험 → `quality.gaps` 와 교차 ④ 이벤트 없이 정상범위를 살짝 벗어난 연결 태그는 미시험 ⑤ 「false = 정비 불필요」로 역해석 안 되게 못박기.
- **프롬프트 최종안(시험 문장 그대로):**
  - 항목 줄: `연결 태그 상태(같은 구간 HH:MM~HH:MM): T1(설명) 정상(범위 안·변동 있음, 구간 a~b단위) · T2(설명) 이상(드리프트·이탈)` / 없으면 `… 태그 마스터에 연결된 태그 없음 — 단독 판단 불가`
  - SYSTEM: 「연결 태그 상태 줄이 있으면 본다. 연결 태그가 모두 정상(범위 안·변동 있음)인데 이 태그 하나만 고착·이탈이면 공정 이상보다 계측기 불량·격리·정비 중일 가능성이 있다 — isolated_suspect 를 true 로 표시하고 isolated_reason 에 근거를 한 문장으로 적는다. 단정하지 않는다('의심'). 연결 태그가 함께 움직였거나 연결 태그 정보가 없으면 false 로 두고 isolated_reason 은 빈 문자열.」 + 「false 는 공정 이상으로 본다는 뜻이지 정비가 불필요하다는 뜻이 아니다.」
  - 스키마: `isolated_suspect`(boolean) · `isolated_reason`(string) 필수
- **한계(발표에서 같이 말함):** 가상 데이터 1근무 · 표본 2회 · prod 미확인. 「계측기 불량을 잡는다」가 아니라 **「주변과 안 맞는 단독 이상을 확인 요청으로 올린다」**.

---

## 4-1. 팀원 독립 시험지로 전후 검증 (13:50 추가)

임도영 프로가 main 에 시나리오를 **24 → 55종**으로 늘렸다(6153037 — Unit 별 Trip 9 · 정비 위장 3 + 파형이 같은 진짜 레벨계 고장 1 · 지침·사고보고서 기반 13 · 정상 5, 정답지에 출처 문서번호). 확장 5근무 실측(4d48a1d · 이슈 #36·#37):
- **엔진:** 포함률 22/22 · 알람 선행 7/9(확장 세트 — 정본 ①② 와 다른 세트)
- **AI 초안(현재 프롬프트):** 정비 3종을 전부 설비 이상으로 읽고 무관한 2건을 인과로 묶음 · 진짜 레벨계 고장은 맞힘 · 정상 5종 중 2종만 정상 · 터빈 Trip 21항목에 「터빈」「Trip」 0회
- **엔진 결함(#36):** 정지 근무에서 외기 보정이 폭주(실측 0.2℃ → −292.9℃) → 유령 드리프트 37건 — `engine/` 이라 정기영 프로 몫. AI 가 이미 「검출기 과보정 의심」으로 스스로 하향

→ §4 의 단독 이상 판정은 우리 가상 시험(2/2)만으로는 약하다. **팀원이 만든 시험지(#34~37 정비·고장 · #51~55 정상 · #29 Trip)로 A(현재) vs B(연결 태그 상태 + 정지 후보·최초 이탈 순서 + isolated/mode_effect/operation 판정) 전후를 잰다 — `probe/probe-ext`.** 자작 수치가 아니라는 점이 발표 근거가 된다. 결과로 흐름 C 프롬프트를 확정한다.
- 남은 결정: #36 엔진 수정 담당·방식(정지 구간을 외기 적합에서 제외 vs 보정량 > 실측 변화면 보정 포기 가드) · `tools/score.py` 가 정상 시나리오(class:normal)를 오탐으로 안 세는 문제 담당

## 5. 문서·GitHub 최신화 (E 흐름) — 조사로 나온 작업 목록

- **정본 먼저:** `docs/공식수치.md` 알람 선행 검출 ① 7/11·+706 → **7/10·+616** / ② +666 → **+560** · **⑧ AI 소요 시간 절 신설**(흩어진 5변형 이력 + 병렬화 후 실측 행)
- **전파:** scenarios(알람 선행·오탐 7→11건·AI 시간) · 체크리스트 · status · HANDOFF · 기획서·검증정리·설명서 rev2
- **심사평 03 직접 지적:** 시연 영상 「64초 무편집·실시간」 표기 → 실제 「4분 34초 · 0:00~6:52 1.5배속(화면 표시)」
- **금지 표현:** 기획서 rev2 「알람이 울리지 않는」 4곳 → 「알람 한계값을 넘지 않은」
- **현재 상태 절:** status·HANDOFF·체크리스트가 본선에서 멈춤 → 결선 절 신설
- **`제출/`(본선 제출 보관본, gitignore)** 은 수정 대상 아님
- **2차(실측 뒤):** 공식수치 ⑧ 병렬화 후 값 · `app/server.py` 의 AI 시간 안내 문구 · engine 문서의 「채택률 미측정」 → 첫 실측치
- **열린 이슈(경모님 OK 시):** #10·#6 닫기 · #35 「대기 근무 1건 상시 확보」로 재정의 · #21 상태 스위치로 부분 해소 코멘트 · #24·#23 엔진 무수정 한계로 유지

---

## 6. 발표자료 (F 흐름) — 투자 프레임 14장

표지 → 문제(표준 4종이 정한 산업 공통 문제) → 실제 사고(대형 사고 + **CSB 완료 조사 133건 중 적어도 8건 6.0%**) → 보고서의 반복 실패 둘 → 알람 기준 대조군 → ENGRA 가 하는 것·안 하는 것 → AI 네이티브 구조 → 동작 실측 → **결선에서 바꾼 것 ①속도 ②사람 기록·채택률 ③단독 이상 판정** → 확장성(수처리 이식 코드 0줄 · 기존 데이터만) → 파일럿 로드맵 → 마무리.
- 비운 칸 `[값 대기]`: 알람 대비 비율 · 속도 전후 · 채택률 · 단독 이상 시험 · 상태 스위치 캡처
- **쓰지 않는 말:** 인수인계 자동화 · 대면 대체 · 알람이 울리지 않은 이상 · 사고의 N%가 인수인계 때문 · 114만 점 5.4초 · 시간 N% 절감

## 7. Q&A (G 흐름) — 44문, `docs/결선/QA_예상질문_v2.md`
가장 아픈 5문(리허설 문장): 전부 가상 데이터 아니냐 · 채택률 없는데 왜 믿나 · 정비·운전모드에서 오검출 나면 · AI 초안이 있으면 사람이 안 기록하지 않나 · 1,000태그 억 단위에서도 되나. 투자자 절 7문. `[값 대기]` 15곳은 공식수치 확정 뒤 채움.

---

## 8. 검증 규율 (모든 코드 PR 공통)
- 격리 워크트리에서 기존 시험 전부 + 새 시험 통과 → codex read-only 반증 무발견까지 → 지휘자 1차 소스 재확인 → PR → 머지
- 속도·판정 수치는 **같은 입력으로 전후 실측**만 공식수치에 올림. 추정은 「추정」 표기
- 라이브 데모 사이트는 머지 후 배포 여부를 따로 결정(배포 권한은 경모님)

## 9. 남은 결정 (경모님)
1. 시연: 병렬화 결과를 보고 **초안 생성까지 라이브**로 할지
2. 열린 이슈 정리(닫기·재정의 코멘트)를 제가 할지
3. 머지 후 **데모 사이트 배포**(서버 반영) 시점 — SSH 는 경모님 쪽
4. `CLAUDE.md` 담당 표(`docs/`=임도영 등)를 결선 기준으로 고칠지
